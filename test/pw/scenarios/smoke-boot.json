{
  "name": "smoke-boot",
  "description": "App boots at phone viewport: tab bar renders, no JS errors.",
  "url": "/music/play/",
  "steps": [
    {
      "action": "waitFor",
      "selector": ".tabbar"
    },
    {
      "action": "assertCount",
      "selector": ".tabbar button",
      "min": 3,
      "note": "SONGS-MERGE phase 1: 3 tabs, not 4 - Library and Setlist merged into one Songs surface with a segmented view switch."
    },
    {
      "action": "screenshot",
      "name": "boot"
    }
  ]
}