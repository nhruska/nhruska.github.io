# UX Sprint 1 - Chaired Plan (DRAFT v1 - pending adversarial review + operator gate)

> Phase-3/5 artifact of the [ux-persona mission](goal-ux-persona-mission-20260703.md).
> Council: 4 seats (P3 beginner, P2 theory, D5 ergonomics, product/roadmap) -> Fable chair synthesis.
> Status: DRAFT v1. Next: codex adversarial (phase 4) -> resolution -> OPERATOR GATE (phase 5).
> Gate protocol: telegram reply "approve" / "adjust: <text>" / "stop" + M3 choice "A" or "B".

## Sprint-1 items (8 core + 1 stretch)

| # | Item | Wave | Effort | Deps | Mechanism ruling | Acceptance (one line) |
|---|---|---|---|---|---|---|
| 1 | S-CLEARGUARD + S-SLOTX (one PR: Compose/Studio destructive safety) | 1a | M | none | Clear = PERSISTENT UNDO banner (F7 interstitial styling), NOT native confirm; resequence Clear away from Save. Slot-x = wireTap movement-cancel + 44px padded hit area. MUST also cover the J3 Studio hand-off screen (rider a). PR body re-runs the destructive-tap inventory across all 7 screens (absorbs S-SAFESCAN). | Clear on Compose AND Studio restores via persistent undo; slot-x >=44px + movement-cancelled; above-the-fold geometry unchanged at 412x915 |
| 2 | S-GOLDEN-A (promote Regime-A theory audit to test/) | 1a | S | none | Current-regime only; 12 Regime-B traps wait for #98 (item 9) | node test/run-all.js fails on any spelling/roman/quality regression |
| 3 | S-NOTABLES (one-shot notable infra) | 1a | M | unblocks 4, 6, 8 | Show-once, dismissible, namespaced localStorage, defensive reads, backup.js schema rules | Reusable by 3 consumers; zero double-fires; backup.test.js green |
| 4 | S-FIRSTRUN | 1b | S | stacks on 3 | Cues anchored to song rows + chord-count badge, NOT toggle chrome (survives M3); soft #98 rebase accepted | Fresh profile sees tap-to-open cue + easy-song nudge exactly once, dismissible |
| 5 | S-SETX (+ rider b: edit-set item-remove 40->44px) | 1a | S | none | KEEP confirm(), ADD wireTap movement-cancel; styled modal + edit-mode relocation -> backlog | Setlist x cannot fire on scroll-grab; item-remove >=44px; no layout shift |
| 6 | S-WHYNOTE (STRETCH) | 1b | S | stacks on 3 | One-shot JIT notable at Compose->Studio hand-off; TUTOR-ROADMAP's own relative/parallel content; static copy + existing display fns ONLY (permitted Phase-4 slice, not the feature) | Teaches relative/parallel "why" for current key/mode, once, dismissible |
| 7 | S-TONES | 2 (post-#98 merge) | S | #98 merged | Consume #98's spelling fn; never duplicate | Chord-detail shows spelled tones (E#dim = E# G# B); tests cover the 12 trap chords |
| 8 | S-ROMAN + S-KEYPOLICY (one PR) | 2 (post-#98 merge) | M | #98 merged; stacks on 3 | Settings row, default Classical Mode-Local; one-time prompt fires JIT at FIRST roman-label surface via NOTABLES - never at first-run; sharp-tie policy note co-located | Lead-Sheet renders bIII/bVI/bVII (unit tests); prompt fires once, only on a roman surface |
| 9 | S-GOLDEN-B (12 professor traps) | 2 (post-#98 merge) | S | #98 merged | Fast-follow to item 2 | All 12 traps green in test/ |

**Swarm scheduling:** Wave 1a parallel (items 1, 2, 3, 5 - worktree-isolated). Wave 1b stacked on item 3's branch (4, 6). Wave 2 fires only on #98's merge event; if #98 is unmerged at sprint end, wave-2 items record DEFERRED-with-reason (compliant completion per goal spec).

## Chair adjudications

| Item | Ruling | Why |
|---|---|---|
| S-ROMAN | IN, wave 2, reshaped | JIT prompt at first roman surface satisfies P2 AND P3; NOTABLES reuse kills the effort/ambiguity objection |
| S-TONES | IN, wave 2 hard-gated | Label surfaces literally being rewritten by #98; building against a moving branch = rebase pain, zero user value now |
| S-CHIPS | OUT -> M3 | M3's unified filter bar replaces this exact chrome next sprint; the 40-44px floor becomes an M3 acceptance criterion |
| S-FIRSTRUN | STAYS IN (P2's cut rejected) | Theory bar already met; cutting the only beginner anchor inverts the persona balance |
| S-EASYFILTER | BACKLOG -> M3 spec | M3's Search+Genre+Key bar dissolves the mode-jargon exposure; encode progressive disclosure as M3 requirement |
| S-SAFESCAN | FOLD into item 1 V&V | An audit, not a product change - one PR-body inventory table |
| S-KEYPOLICY | FOLD into item 8 | Policy blurb is not an atomic PR; P2 asked for co-location |
| S-INVERSIONS | BACKLOG | Post-#98 coverage expansion, no user surface this sprint |
| S-WHYNOTE | IN as stretch | Only item serving P4's "why" JTBD - the north-star persona - at S effort on infra already being built |
| Riders a + b | FOLD (mandatory) | Studio screen hazard recurs verbatim; 44px edit-set is one line in the same file |

## M3 - operator gate choice (O3 eligibility exercised, evidence says decline)

All four seats independently: NO M3 slice. Chair concurs - every buildable slice hits the songbook.js/tracks.js chrome that BOTH #98 rewrites and M3 restructures (three-way rebase collision), and the mission bars merge-colliding items.

- **A (RECOMMENDED): zero M3 slices.** M3 = sprint-2 primary, fresh session, post-#98, with finder-rehome 5A/5B pre-resolved (5A recommended in the friction log).
- **B: down-payment only:** a spec+fixtures PR (merged-repertoire dedup rules + failing test fixtures, zero runtime code) - collision-free.

## Backlog register

M3 (sprint-2 primary) - S-CHIPS (into M3 bar) - S-EASYFILTER (into M3 spec) - S-INVERSIONS (post-#98 golden coverage) - SETX phase 2 (styled modal + edit-mode home) - F6 (#98 verification rider) - Full Phase 4 decoupled solo-scale selector.

## Risks handed to the adversarial reviewer

1. Stacked draft PRs under zero-merge (base staleness, merge-order, never-delete-stacked-base discipline)
2. NOTABLES localStorage schema vs backup.js SCHEMA_VERSION/MIGRATIONS; double-fire across 3 consumers
3. CLEARGUARD undo-state correctness vs mode re-harmonization, Save, navigation-away (enumerate loss vectors)
4. One-screen-budget regression from 44px padding + Clear resequencing on the fixed Compose top (geometry-diff required)
5. Wave-2 coupling to #98's API seam (one named seam only) + WHYNOTE scope creep into Phase-4 feature logic

---

# v2 - Adversarial resolution (2026-07-03, phase 4 complete)

Codex verdict on v1: REQUEST_CHANGES, 12 findings (4 critical / 6 high / 2 medium). ALL FOLDED as binding spec amendments below. Resolution rulings by mission orchestrator (Opus); chair-level judgment was only required on the undo contract (A3).

| # | Finding (sev) | Amendment (binding on implementers) |
|---|---|---|
| A1 | Stacked-PR topology (crit) | Stack protocol: items 4/6/8 target item-3's branch until it merges; after merge, retarget to main + rebase; item-3 branch is NEVER deleted until the stack is clear (stacked-base rule); each stacked PR body names its base + tested base SHA |
| A2 | Wave-1a false independence (high) | File-ownership map required pre-spawn: item 1 owns Compose/Studio action-bar + prog-strip regions of songbook.js/css; item 5 owns setlist header + edit-set regions; overlapping region = re-sequence, don't parallelize. Post-both-merged geometry re-check at 412x915 |
| A3 | Undo semantics undefined (crit) | CONTRACT: undo snapshot = full pre-Clear state (progression array + cTpose + songKey). ANY subsequent mutating action (add chord, transpose, mode change, key change, Save) invalidates it. Banner is route-local, in-memory, session-only - dies on tab-switch/reload (matches edit-set precedent; zero backup.js surface). Banner persists until invalidation, not a timed toast |
| A4 | Notables schema (crit) | ONE versioned key `music.notables.v1` holding an object of consumer-id -> dismissed-timestamp; claim/dismiss API with atomic consumer IDs + priority (only ONE notable may render per tick; first-run > whynote > roman); backup tests for old/missing/corrupt/future shapes; SCHEMA_VERSION bump NOT needed (additive key) - documented in PR body |
| A5 | Double-fire (high) | Covered by A4's single-render-per-tick claim API + tests simulating simultaneous consumer init |
| A6 | Undo persistence hand-wave (high) | Resolved by A3: session-only, route-local, explicit |
| A7 | Fold-budget unenforceable (crit) | Geometry-diff GATE: automated Playwright check at 412x915 asserting the Compose fixed-top region's bottom edge is unchanged (+-2px) before/after items 1+5 TOGETHER; screenshot pair in both PR bodies |
| A8 | #98 seam unnamed (high) | Named seam: wave-2 items consume ONLY `Circle.spellKeyAware` / `Circle.spellScaleKeyAware` / `Circle.keyLabel` (verified present on #98's branch). If #98 merges without them, wave 2 is BLOCKED-not-improvised; record DEFERRED |
| A9 | WHYNOTE scope (med) | Restricted to static templates interpolating ALREADY-RENDERED key/mode labels; zero new theory derivation; any conditional beyond string choice = out of scope |
| A10 | SW cache bumps (high) | EVERY PR touching a CORE-listed file bumps `music/sw.js` CACHE in the same commit; acceptance-criteria line added to every item; stacked PRs coordinate: LAST-merged PR of the stack owns the final bump (each PR still bumps; merge conflicts resolved by taking max+1) |
| A11 | localStorage test isolation (med) | test/ helper: per-test namespace reset for `music.notables.*` + any new keys; item 3 ships the helper |
| A12 | Stale CI on stacked drafts (high) | Each stacked PR re-runs CI after final base update; PR body records tested base SHA; sprint-complete = all 9 PR bodies show current-base CI green |

**Status: v2 RESOLVED - AT OPERATOR GATE.** Reply: approve / adjust: <text> / stop, plus M3 choice A (recommended) or B.

---

**Approved-by: Nik** - 2026-07-03, in-session selection "1" (= approve + M3 choice A: zero M3 slices; M3 becomes sprint-2 primary). Gate closed; wave-1a execution authorized (items 1, 2, 3, 5 parallel; 1b stacks on item 3; wave 2 awaits #98 merge).

---

**Mission close (2026-07-04):** Sprint-1 core COMPLETE - items 1-6 (+ operator-injected S-MODAL, S-SETTINGS) merged and live (v78). **Wave 2 DEFERRED-with-reason** per the goal spec's compliant-completion clause: items 7 (S-TONES), 8 (S-ROMAN+S-KEYPOLICY), 9 (S-GOLDEN-B) hard-depend on PR #98 (key-aware spelling), which is still open at mission close. They resume in a follow-up session on #98's merge event using this plan's specs verbatim. M3 = sprint-2 primary (fresh session, 5A/5B pre-resolved by operator).
