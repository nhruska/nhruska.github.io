# Mission Queue - nhruska.github.io (music app)

> The plan-ahead pipeline: agents are never dormant while quota exists and SHORT is non-empty.
> Horizons: NOW (in flight) / SHORT (interviewed + gated = LAUNCH-READY) / MID (vision known,
> needs spec interview) / LONG (idea, needs vision interview). Active-stance reads this file
> at session start; when capacity frees and SHORT has items, the top one launches (trust
> rules per quality profile) with an INFO ping. Operator batches interviews to refill SHORT.

> **RECONCILED 2026-07-11, fifth pass (overnight /goal close):** live version is **music-v155**. The overnight arc shipped M-13 end to end (#241 LZ v151, #242 g1 v152, #244 g2 v154, #245 g3 v155) plus M-COMPETENCY (#243 v153 - M-14's core), the Cockpit remix pick (#239/#240) and the ps-internal LZ handoff. Two CE frictions captured below (CE-WORKTREE-PREFLIGHT, CE-APP-DROPPED-INPUT).
>
> **RECONCILED 2026-07-10, fourth pass (swarm close):** live version is **music-v138** (Calluses v136, G3 v137, teardown v138); Deck missions #205-#217 shipped the same day. The clear-the-chessboard mission finished its build arc: #197 + #198 + **#199 (FORK-4 REMOVAL - key-aware spelling, all 12 professor traps enforced)** MERGED; #98 + #70 CLOSED as superseded; #88 (M-3 strum, draft) is the one legacy PR open. Usage flows are committed declarative PW scenarios ([test/pw/](../../test/pw/README.md), 8 green, pixel gates in docs/artifacts). Mission goal spec + audit: [goal-clear-chessboard-20260710](goal-clear-chessboard-20260710.md).

## NOW (in flight)

| Mission | State | Spec |
|---|---|---|
| **M-LIB-BAKE: playlist extraction + player-feel** | **PR-A [#322](https://github.com/nhruska/nhruska.github.io/pull/322) MERGED (v322); PR-B [#323](https://github.com/nhruska/nhruska.github.io/pull/323) MERGED (v323-8) 2026-08-09** after 7 phone-UAT batches - the app now behaves as a real music player (SSOT bar, sheet+live tabs, Jams filter, queue + prev/next/shuffle, honest state sync, Media Session). **PR-C [#324](https://github.com/nhruska/nhruska.github.io/pull/324) MERGED (v324) 2026-08-09**: item 17 + 14 artists enriched. **PR [#328](https://github.com/nhruska/nhruska.github.io/pull/328) OPEN (v328-5)**: shuffle-in-bar + solid SVG transport, jams-first landing + tour-ends-in-a-jam + no-scroll tour + cigar box retired + welcome-jam ad strategy (reggae original). Veto #8 C-vs-Am stands open. | [goal spec](goal-playlist-extraction-20260806.md) |
| **M-PLAYLIST-IMPORT: user-facing "import your playlist" + variety fill** | **Import MERGED 2026-08-09 (PR [#326](https://github.com/nhruska/nhruska.github.io/pull/326), v326-2)** after live-device UAT: keyless enumeration + key ladder + landing-on-results + repeat-URL dedupe all proven. **Variety fill MERGED 2026-08-09 (PR [#327](https://github.com/nhruska/nhruska.github.io/pull/327), v327-3; catalog 55->108 incl. the 46-item operator playlist + 15 searched Phish keys)**: 12 curated search-first gap fillers (Bm/G#m/B/Eb/Ab + A/G/D/Bb blues + tempo tails; ladder-dogfooded 12/12; candidates gate gains the explicit 'search-first' lifecycle carve-out). Residual gaps (C#/F# major, D#m/A#m, outer blues keys) deliberately left for the OPERATOR'S curated gap playlist - the pipeline bakes it + enriches ids onto the stubs via trackKey remaps when ready. Also 2026-08-09: community PR [#274](https://github.com/nhruska/nhruska.github.io/pull/274) (emre155, first outside contribution) merged via vehicle #325 (v274). | [goal spec](goal-playlist-import-20260809.md) |
| **S-SDD-PILOT: "Calluses" (F Mixo/Dm)** | **STORED 2026-07-10 (v136)**: full SDD loop closed - spark -> skeleton -> v0.1 -> operator reframe burst -> v0.2 -> "store it" -> songs.json (first operator-authored catalog song, library-scenario-proven). Taste verdicts encoded into songwriting-coach; next round = play-and-burst on feel | [sdd-pilot-friction](sdd-pilot-friction-20260710.md) |
| **FORK-4 removal - key-aware render wiring** | **SHIPPED 2026-07-10 (PR #199, v133)**: every keyed surface spells letter-per-degree from `preferredTonicName` (bVII of C = Bb; A#-maj renders Bb-maj); token-vs-display split keeps packs/audio/storage canonical; 12 professor traps enforced as tests; PW pixel gates re-proven. Wave-2 residue queued below (S-KEYPICKER-FLATNAMES). | [PR #199](https://github.com/nhruska/nhruska.github.io/pull/199) |
| (v121 snapshot - superseded, kept for trace) #98 key-aware spelling was the stalled linchpin gating S1+S2; resolved by re-implementing its core fresh in #198 and closing #98 | CLOSED 2026-07-10 | [PR #98](https://github.com/nhruska/nhruska.github.io/pull/98) |
| (v113 snapshot - superseded, kept for trace) idle - EAR-FIRST ARC night shipped v106->v113: audition+retarget+lights+full-neck, tempo+legend+accent-palette, jam matrix+yt-prefill, dim shapes, feedback reconcile w/ countdown undo, keyless playlist pipeline. AWAITING OPERATOR: bake veto + anchors veto -> anchors UI wave; wave-2 candidates: A/B compare, tempo, degree speech; follow-up: tab-switch icon reset | [vision-ear-first](vision-ear-first-20260704.md) |

## SHORT (launch-ready - gated, fire when capacity frees)

| # | Mission | Trigger / launch condition | Spec state |
|---|---|---|---|
| S1 | Wave 2: S-TONES, S-ROMAN+S-KEYPOLICY, S-GOLDEN-B | **UNBLOCKED 2026-07-10 - #199 merged.** Note: S-GOLDEN-B/S-KEYPOLICY specs may be partially satisfied by #199's professor-trap enforcement - re-read the spec against main before launching | Fully specced in [ux-sprint-1-20260703.md](ux-sprint-1-20260703.md) items 7-9 + amendments - APPROVED |
| S2 | Sprint 2: M3 songs/tracks full merge | **5A RESOLVED (operator, 2026-07-04: dissolve the finder - curation to +Add/per-item edit, circle panel into the Studio)**; remaining gate: ~~FORK-4 wiring~~ (#199 MERGED) + 30-min spec-formalize interview | Build plan in [UX-FRICTION-LOG](../../music/docs/UX-FRICTION-LOG.md) M3 section; council + chair sequencing decided; needs a 30-min goal-interview to spec-formalize -> then launch-ready |
| S3 | S-BLUES-BOXES: named box positions 1-5 (root string + start fret + neighbor-move) on the Studio scale view | Operator gate on a drafted spec (P5-demanded; drafts from solo-scales.md + posWindow) | Spec DRAFTED: [short-specs-20260704.md](short-specs-20260704.md) - one keyword to launch |
| S4 | S-DIAGRAM-PREF: expertise-adaptive display (dots vs clean patterns + shape-classifier step 0) | **REVIEW-READY: spec already drafted + P5-folded** - 5-min read, then one keyword (go S-DIAGRAM-PREF) | [short-specs-20260704.md](short-specs-20260704.md) + canonical detail in the wiki expertise-adaptive-display page |
| S5 | S-SAVE-TRUTH: silent-fail saves fixed (quota detect + rollback + real failure message on all 5 save paths, backup.js applyAtomic pattern) | **go save-truth** | Spec = [analysis A1](analysis-refactor-enhance-20260704.md) - the verdict's top pick (P1 data-loss trigger) |
| S6 | ~~S-HARDEN~~ FIRED -> NOW (2026-07-04): wireTap dedup x4->1, escHTML dedup x8->1, SW CORE/CACHE verify script wired into tests | **go harden** | Spec = analysis A4+A5+A6 + toggleSet 'Added to setlist' unconditional-toast fix (same A1 bug shape, flagged in #116) + SW verify script asserts CACHE differs from origin/main when shared/ files changed (identical-string v83 collision, PR #117) |
| S8 | S-BACKUP-NUDGE: one-shot Notable "back up your library" via free priority slot + backup.describe() | **go backup-nudge** | Spec = analysis B2 (pairs with S5) |
| S9 | S-CHIPS-PLUS: P5 enhancement asks - Dom7-arpeggio or Mixolydian chip in the Blues-key Compose preview + degrees line under the preview notes; evaluate bar-by-bar target emphasis (3rd/b7/root) remainder after ghost dots | **go chips-plus** | Spec = P5 W3 verdict items (fold doc); ghost dots + card rewrites already folded in-mission |
| S10 | S-LOCAL-JAM: fully-owned first-jam audio - operator records a 60-90s ORIGINAL jam loop (guitar/uke, ~1MB mp3); app gains a local-audio track type (t.audio, <audio>-backed Studio transport, SW-cached, honest state we fully control); the welcome jam swaps to it for an offline + ad-free-forever first play. Downloading/bundling YouTube media was considered and REJECTED (public repo = redistribution + YT ToS). | Operator records the loop, then **go local-jam** (2026-08-09 round-4 fork: "keep YouTube, reggae stays welcome" chosen for now) | This QUEUE row is the spec seed |
| S11 | S-TAP-TEMPO: match the synth loop's tempo to the playing backing track - a TAP-TEMPO control (tap 4+ beats on a button, median inter-tap interval sets the loop bpm; stretch: align loop phase to the last tap so downbeats land together). Operator round 7: "can we try to match tempo of playing song?? we used to have a tap to sync feature... would be better than default settings". Route through audio-dsp-coach for the timing math; replaces nothing (Slow/Med/Fast stays the fallback). | **go tap-tempo** | This QUEUE row is the spec seed |
| S12 | S-GUIDE-CONTEXT: rethink the ? guidance CTA - operator round 7: "I want the guidance, but it's not a good CTA and could be guidance elsewhere or served individually based on the song/scale". Direction: per-song/per-scale guidance served contextually (e.g. a one-line mentor hint under the scale that deepens on tap) instead of a generic ? icon; composes the existing SoloGuide/mentor-card content, pedagogy-coach owns the grading. | Operator picks the direction, then **go guide-context** | This QUEUE row is the spec seed |

| S13 | ~~S-AGENT-FLOOR~~ **SHIPPED 2026-08-17** (#332, music-v332): bundled AGENTS.md in the skills zip, `music/agent/` manifest + consistency gate, round-trip test | done - swarm wave 1 | [goal-agent-interaction](goal-agent-interaction-20260817.md) |
| S14 | ~~S-JAM-LINK~~ **SHIPPED 2026-08-17** (#332, music-v332): `?jam=` deep link, ephemeral overlay + Save handoff | done - swarm wave 1; Studio re-home follows as S15 | [goal-agent-interaction](goal-agent-interaction-20260817.md) section 4b |
| S15 | ~~S-JAM-STUDIO~~ **SHIPPED 2026-08-17** (#333, music-v333-2): jam links open the real Studio (ephemeral track via jamTrackFromSetup); bespoke overlay retired; shared links now survive a fresh device's welcome tour (param carry + NavHistory-unwind defer) | done - wave 2 | [goal-agent-interaction](goal-agent-interaction-20260817.md) |
| S16 | ~~S-PLUGIN~~ **SHIPPED 2026-08-17** (#333): plugin/music-coach - 4 skills, 3 commands, agent profiles music-coach/jam-dj, mutation-tested sync gate | done - wave 2 | spec section 4c |
| S17 | ~~S-SETUP-DOC~~ **SHIPPED 2026-08-17** (#333): music-setup/v1 import, JamLink.parse reused as the one validator | done - wave 2 | spec section 4b |
| S18 | ~~S-ANALYTICS-ARTIFACT~~ **SHIPPED 2026-08-17** (#334, music-v334): `/music-coach:report` + slot template + gate; [sample artifact](../artifacts/music-report-sample-20260817.html) | done - wave 3 | [m-agent-arc](m-agent-arc-20260817.md) |
| S19 | ~~S-SW-XORIGIN-DEADLINE~~ **SHIPPED 2026-08-17** (#334): cross-origin fetches race NET_DEADLINE_MS; parser can never freeze on a hung font | done - wave 3 | [m-agent-arc](m-agent-arc-20260817.md) |

### Newly queued (findings)

| # | Finding | Source |
|---|---|---|
| F-JAM-GENRES-MULTI | **SHIPPED 2026-09-01 (PR #342, v342-4, operator burst)**: jam-finder genre chips are MULTI-SELECT (toggle, min 1, query joins selections in order; tempo stays single); + jazz/smooth jazz/funk/disco/soul/r&b added per-scale where the songwriting-coach read supports the fit (all six in pentMinor - the operator's context; pentMajor/blues honestly unchanged). Red-first scenario jam-genre-multi.json; single-genre behavior locked byte-identical | operator UAT 2026-09-01 ("genres - add jazz, smooth jazz, funk, Disco... allow multi select") |
| S-AUDITION-CAPTURE | **SPEC'D 2026-09-03, awaiting operator pick (operator: "the chords shown under the circle of fifths have been a surprisingly nice tool for me to write progressions before using compose tab. what is the pattern here we can enable?")**. Pattern named: **audition-first composing** - the Studio's chords-in-key strip is a PALETTE (zero commitment per tap, 7 chips all correct by construction, auditioned against a live key, circle co-present) where Compose is a CANVAS (commit-first). The gap: `renderChordChips()` plays the tap and DISCARDS it, so an ordered by-ear chord sequence - real creative output - is lost and re-entered by hand. Proposal, ranked: (1) **tap-trail capture** - remember the last N taps, offer "Keep these N -> Compose" via the existing seedKey bridge, trail clears on key change/close; (2) **name the pattern** - match the trail against songwriting-coach's proven families via `Circle.romanFor` and say "that is the Axis loop" + offer the canonical completion (teaches while he plays); (3) next-chord emphasis on the strip, **emphasis ONLY, never reordering** (the strip's calm comes from the 7 chips never moving - ux-coach recognition-over-recall); (4) audition-vs-commit mode in Compose = operator vision call, not assumed. Recommend building 1+2 as one feature. Full analysis: [design-audition-first-composing-20260903.md](design-audition-first-composing-20260903.md) | operator question 2026-09-02 |
| S-SETLIST-GESTURES | **SHIPPED 2026-09-03 (PR #342, v342-7, operator UAT: "change drag to press and hold on song on setlist" + "change setlist delete to slide swipe - slides ~25% to reveal delete intent - like outlook and Gmail mobile. both directions are delete")**: the set row adopts the standard mobile-list grammar - tap plays, press-and-hold on the ROW lifts for reorder (300ms/6px slop, matched from wireSectionDrag, not reinvented), a >=25% horizontal swipe either direction deletes through the SAME removeFromSet+undo path, with a first-few-px directional lock so a vertical scroll never moves the row and a swipe never fires tap-to-play. `.li-grip` and the always-on arm-x are retired from set rows (no other consumer rendered them). **a11y (WCAG 2.5.1 - both new gestures are pointer-only):** up/down/remove stay in the DOM per row, quiet at rest (`.li-a11yctrl` skip-link reveal-on-focus), full-size on keyboard focus, remove keeping the two-tap arm so a stray Enter never destroys a row - all VERIFIED by keyboard, not asserted (setlist-a11y-fallback.json, which replaces the retired setlist-remove-arm.json rather than deleting its coverage). Agent's own pw run caught two real CSS bugs pre-merge: a `min-width:44px` defeating the 1x1 hide trick, and `.li-inner` hugging content so the swipe underlay peeked at rest | operator UAT 2026-09-02 |
| S-SETTINGS-ACTIONROW | **SHIPPED 2026-09-03 (PR #342, v342-10, operator UAT batch 5: "too cluttered w text. simplicity FTW. UI standard layout? for export etc buttons, min prose").** There was no action-row convention, so six rows doing the same job had been invented at their own call sites in FOUR looks - a bordered .setRow card with a stacked description, an .aboutLink with an icon and a wrapping sentence, a .btn.ghost pair, and a raw URL as body text. The standard is now written in component-conventions.md ("Action rows": ONE ROW = ONE ACTION, verb-first label on one nowrap line, no per-row description, at most one caption per group, .saMeta for live state only, external rows take --brass ink + the leave-app glyph) and implemented as the single .setAction primitive; the four lookalike CSS rules were DELETED rather than left dormant, since a dead lookalike is what the next author reuses. Per machine-ssot-enforcement the gate ships in the same commit: test/settings-action-row-lint.test.js fails on a description inside an action row, on stacked captions, on a reintroduced lookalike class or rule, and on a URL rendered as body text. Evidence: 69/69 node files green, settings-action-rows scenario PASS and mutation-proven RED twice (ghost button restored, nowrap guard removed), the lint mutation-proven RED catching all three defects at once, settings-agent + settings-accordion-affordance + settings-skills-uat green after being repointed off the retired classes, live 412x915 both themes - five uniform 48px rows, zero clipped labels, zero console errors. |
| S-SETTINGS-AFFORDANCE | **SHIPPED 2026-09-03 (PR #342, v342-9, operator UAT batch 4: "settings text too similar to sections" / "accordion expand handle not mobile std" / "accordion items need individual distinction...not clear it's accordions" / "remove theme text desc" / "skills (and other settings titles/descs). simplify, functional purpose, concise").** The sections were flush rows split by a hairline - the visual grammar of a LIST, so nothing said they open. Each is a grouped card with a 10px gap now (the mobile settings convention, iOS and Material both), the header row is 52px, and the handle is a stroke chevron drawn from borders rather than a filled U+25BE, which is a desktop select glyph. Hierarchy: .setLbl sat at 600/~1rem against an .accBtn at 800/1.08rem - too small a step to read as a hierarchy, so "Theme" looked like a peer of "Appearance". It takes the app's existing caption convention (.rf-lbl / .progLbl family: .68rem uppercase micro-caps in --txt-dim) rather than a new lookalike. Copy: the Theme description is deleted (the segmented control already names its three options); Skills, AI Agent and Backup lead with what the thing IS and what it is FOR, inside the copy-coach beginner vocabulary budget, no semicolons. Evidence: 68/68 node files green, settings-accordion-affordance scenario PASS and mutation-proven RED twice (flush rows restored, triangle glyph restored), settings-agent + settings-skills-uat still PASS, both themes render-verified at 412x915 with zero console errors. |
| S-DETAIL-SOLO-CONSISTENCY | **SHIPPED 2026-09-03 (PR #342, v342-8, operator UAT batch 3 items 4+6: "make song details and solo view consistent layout. same back button SSOT behavior and location... song details has ... button fly out menu - use consistently on Solo view" + "large chord chips on song details view - make them clickable like all others").** The two backs were one meaning wearing two looks (accent-filled .backArrowBtn vs a bare .iconBtn); .backArrowBtn is now an app-wide primitive both compose. The Studio fly-out trigger stopped impersonating the app Settings gear (hamburger in the gear's slot on a screen that COVERS the appbar - the operator flagged that slot twice) and is the song view's .iconBtn.moreBtn. The collapse Back performs silently is now a NAMED fly-out row rather than a second top-level button that would duplicate Back. Large sheet chord chips became real buttons carrying the canonical token, wired through ONE extracted wireChordTaps shared with the compact row (the pointerdown-immediacy contract lives in one place, not two); Stage's copies are `disabled` so the perform surface gains no lying affordance. Evidence: 68/68 node files green, detail-solo-consistency + solo-topbar-overflow scenarios PASS and mutation-proven RED both ways, live measurement on a 412x915 device shows identical fill/ink/radius/box across both backs and 48x73 chips at 27.2px with zero app console errors. |
| F-VIDEO-ADLIKELY | **SHIPPED 2026-09-02 (PR #342, v342-6, operator UAT: "can we detect not playing anything -> playing (first load or idle time) and show the yt video - most likely to have ads. not between every song tho")**: the video shows itself exactly where a pre-roll is likely - an AD-LIKELY open (nothing was playing AND first-play-this-load, or the last stop was over VID_IDLE_MS=10min ago) opens the theater; every other open CARRIES the state the user left it in, so the video never jumps up between songs and is never yanked away (round 16 stands). Ad-likely BEATS a stale stored pref - the operator has twice asked to SEE the video when ads are likely. The rule is a PURE predicate `TracksModel.adLikelyOpen({wasPlaying, hasPlayedThisLoad, lastStopAt, now, idleMs})`, behaviour-tested across all four states + both idle boundaries (a scenario could not discriminate it: on a fresh device the round-16 fallback is also 'theater', so the browser test passed under a forced adLikely=false - extracting the seam is what made the rule provable). Mid-session carry proven on pixels in video-adlikely.json. VID_IDLE_MS is a judgment call, not measured - one named constant to tune | operator UAT 2026-09-02 |
| F-VIDEO-NO-AUTOHIDE | **SHIPPED 2026-09-02 (PR #342, v342-5, operator UAT: "don't auto hide yt video. I keep having to open it to skip after ads")**: THE APP NEVER HIDES THE VIDEO ON ITS OWN. Round 15's open-PARKED default + the 15s auto-demote countdown (and its Keep-open / Minimize-now chrome + CSS) are removed; a fresh open shows the video so YouTube's Skip is reachable with no tap. Round 15's intent survives as a REMEMBERED CHOICE (`music.vidPref.v1`, additive - park once, every later open is parked, until you show it again). Root cause named: this was the SECOND ad trap - a 7s wall-clock collapse (2026-07-31) was mitigated to a 15s playback-anchored window, and round 15 silently undid that mitigation. The ad boundary is undetectable cross-origin, so no timer can be tuned safely around it. Red-first scenario (now video-adlikely.json) fails on pre-fix code at the fresh-open assert; video-pip choreography updated. **AMENDED by F-VIDEO-ADLIKELY (round 17) below** | operator UAT 2026-09-02 |
| S-BACKING-SOURCE | **RESEARCHED 2026-09-02, verdict: archive.org is NOT the answer (operator todo: "find royalty free backing tracks on archive.org to replace current set")**. Findings: (1) no archive.org item found carrying BOTH a confirmed license AND a stated key/bpm - the app's HUD treats key+mode as load-bearing, so an untagged track means ear-tagging every one (same curation burden as today, worse discovery); (2) licensing is murky - the Archive disclaims verifying uploader rights, so a user re-upload is not a license grant; (3) ARCHITECTURE: the Studio is built on `t.yt` + an `<iframe>` embed, so ANY direct-audio source needs a new track type (`t.audioUrl`) + a parallel `<audio>` player path - a real feature, not a URL swap, and exactly what **S-LOCAL-JAM** (operator-records-his-own-loops) already scopes. Streaming mechanics UNVERIFIED (archive.org egress blocked in-container - `<audio>` hotlinking likely fine since CORS only gates Web Audio, but untested). Recommendation: don't build against archive.org; S-LOCAL-JAM is the path with no open legal questions. Cheapest next step if pursuing anyway: operator opens 3-5 candidate identifiers in a browser and checks for a real CC/PD badge + audible key | operator todo 2026-09-02 + scouting agent report |
| F-MODEFLIP-DEGREES | **SHIPPED 2026-09-01 (PR #342, v342-4 - operator 'go')**: convertProgressionQualities now maps by SOURCE-mode degree INDEX for the 7-step modes (C-G-Am-F Maj->min = Cm Gm G#/Ab Fm, exact round-trip; Blues paths byte-identical; D3 leave-alone now only for genuinely chromatic source roots). romanFor labeling of chromatic leftovers deliberately deferred (chromatic-major-relative convention is internally consistent). WAS: **Parallel mode flip strands non-degree chords and then MISLABELS them (operator UAT 2026-09-01: C-G-Am-F, C Maj -> minor, "doesn't seem accurate"; music-theory-coach consulted).** Verified on the SSOT: convertProgressionQualities maps each root by membership in the TARGET mode's steps (recorded decision D3), so C G Am F -> Cm Gm **Am** Fm - the Am (offset 9, not an aeolian degree) stays literal - and Circle.romanFor(Am, Cm) then labels it a clean "vi" although C minor's 6th degree is Ab (same numeral-contradicts-note class as S-KEY-SPELLING). Coach expectation + fix shape: (1) map by SOURCE-mode degree INDEX -> same index in target mode (I V vi IV -> i v VI iv = Cm Gm **Ab** Fm) - symmetric, so round-trip stays clean; genuinely chromatic source roots keep the D3 leave-alone; Blues palette carve-outs (offsets 0/5/7) preserved; (2) romanFor never asserts a plain diatonic numeral for a non-degree root - mark it borrowed/chromatic. Reverses recorded decision D3, so operator-gated: **go mode-flip** | operator UAT 2026-09-01 + theory-coach probe (this session) |
| F-SW-XORIGIN-DEADLINE | sw.js cross-origin branch (fonts/icons) has NO network deadline: a cache-miss + a hanging (not rejecting) network - bars-with-no-data, or a stalling proxy - blocks a render-blocking `<link>` and freezes page parse indefinitely. Same-origin got the NET_DEADLINE_MS fix for exactly this class; cross-origin never did. Pre-existing (not a wave-2 regression); surfaced during wave-2 render-verify when the container proxy hung the Google Fonts fetch under SW control. Fix shape: apply the same deadline race (serve cached-or-error past ~3.5s) to the cross-origin branch | wave-2 integration verify 2026-08-17 - FIX SHIPPED wave 3 |

| S-ENFORCE-2B | **SHIPPED 2026-09-01 (PR #342, v342-2)**: the LAST two native confirm() sites (songbook delete-custom-item, repertoire-form delete) moved to the ONE arm-to-delete grammar (clear-setlist had already migrated); no-native-dialog lint now pins BOTH files at ZERO - the native-dialog debt register is closed. .helpIcon applied to the Studio scale-guide toggle | PR #170 inventory -> PR #342 |

| # | Item | Source |
|---|---|---|
| S-NAVHIST | **CLEARED-STALE 2026-07-10 (swarm, zero code)**: the double-pop fix already shipped inside #197's settleAfter + deferred-deliver machinery; solo-skip + solo-cancel scenarios both green | PR #144 finding, evidence in swarm audit |
| S-POSTPROG-FLOW | **Post-progression workflow unclear (any experience level).** After adding a suggested progression, the Solo choice prompt (Save & open Studio / Skip) + the "Solo" button both appear - a new user is curious about Solo, felt they "can't cancel", and had no clear read on the next step. **"Can't cancel" FIXED in PR #197**: the Solo modal gained a visible **Cancel** and dismiss gestures (backdrop/Escape/Back) now stay on Compose instead of navigating into the Studio. REMAINING (needs a vision call): the broader intended after-you-build flow - should Solo/Save auto-surface at all, what is the ideal next-step guidance for a new user. | operator UAT 2026-07-10 (PR #197 preview) |
| S-DELHANDLE-OVERFLOW | **Compose delete-handle overflows the chord card (1-3 chords).** In the `full` diagram-card stage, the per-slot `x` remover floats above/outside the card bounds (disconnected from the chord it deletes). Contain it to the card. **FOLLOW-UP FIXED 2026-07-10 (S-DELHANDLE-CENTER, keypicker branch, v142)**: the x glyph was padding-nudged off the circle's center - content box now carved to the exact 24x24 circle + flex-centered, true by construction at every stage; prog-delete-handles scenario green. | operator UAT 2026-07-10 |
| S-PROG-FIT-6 | **4-6 chords should fit with NO vertical scroll - "so close to fitting".** The fill-row/grid stages nearly fit the fixed region at 5-6 chords but tip into scroll. Tighten card/gap sizing so 4-6 fit above the fold (A7 geometry gate at 412x915). **VERIFIED 2026-09-01 (PR #342)**: the committed prog-fit-6 A7 geometry gate ran GREEN at 412x915 (6 chords built, #catChips fully in-viewport, screenshot eyeballed) - the PR #198 trim holds; no further CSS needed. Residual: operator taste-tap on a real phone. | operator UAT 2026-07-10 |
| S-TYPEFILTER-ACCENT | **All-view Maj/Min/7th selected chip is full accent CTA - reads as friction.** The selected type-filter chip is `.chip.on` (accent fill, D-SELECTED-ACCENT). Operator feels it shouts. THIS IS THE D-SELECTED-ACCENT reconsideration flagged in the visual-language design: a secondary FILTER's selected state competing with the one primary. Decision needed: reverse D-SELECTED-ACCENT (selected = outline, fill reserved for primary) app-wide, or a filter-specific quieter selected treatment. | operator UAT 2026-07-10 |
| S-CLEAR-INKEY | **After Clear, the view stayed on a stale "All" pin instead of returning to In-key.** With D-DEFAULT-C the initial state is In-key on C major, but Clear did not reset the view pin - if the user had tapped All while composing, that pin persisted over the fresh canvas. FIXED: Clear now resets chordView to follow-the-key + rebuilds the palette -> In-key on the default key. | operator UAT 2026-07-10 (missed msg) |
| S-SUGG-DIFFERENTIATE | **SHIPPED 2026-09-01 (PR #342, v342-2)**: ranked suggestion chips carry a sugg-reco marker dot (shape + accent tint, not color-only); browse-palette chips (ccChip) toned neutral/quiet; ranking untouched, no new hue; DOM tests lock the two surfaces apart | operator UAT 2026-07-10 -> PR #342 |
| S-SOLO-SCALE-DEFAULT | **Studio solo-scale picker should be theory-aware + emphasis-laddered.** Landing in the Studio (Solo practice, C major from a Compose progression), the "SOLO OVER IT" picker (Ionian / Pent major / Pent minor / Blues) defaults to **Pent minor** - wrong best-choice for a MAJOR-key context (Ionian or Pent major is the natural home over major). Per operator: (1) default-select the theory-BEST scale for the incoming key+mode, (2) OUTLINE the other picks (visual-language emphasis ladder - one filled primary), (3) disable/remove incompatible options where applicable. Best-scale + compatibility logic to come from the new **music-theory-coach** skill. **FIXED (PR #198)**: the picker now default-selects the theory-best pentatonic (major-family key -> Pent major, minor-family -> Pent minor, a Blues key keeps its own blues scale) via the music-theory-coach rule, and does a full select() so the fretboard/notes/guide render that scale too (not just the chip highlight). Part 2 (outline others) is the existing chip emphasis ladder - the one selected chip fills, the rest stay outline. Part 3 (disable incompatible) is n/a: over a diatonic key all four scale choices are playable (major/minor pentatonic interplay is a feature, not an incompatibility), so nothing to disable. **EXTENDED (PR #198)**: (a) the default is now PROGRESSION-AWARE - `Tracks.inferSoloDefault(key,mode,seq)` reads the actual chords via `Circle.romanFor` and upgrades the default to Mixolydian (a bVII-major tell over a major key) or Dorian (a major-IV tell over a minor key), else the safe pentatonic. (b) Mixolydian + Dorian are now SELECTABLE context chips (major key -> +Mixo +Dorian, minor key -> +Dorian, deduped against the key's own mode, none on a Blues key) - their `SoloGuide.card` coaching already shipped. Chip visual + fit need a phone tap on the branch preview. **SKIP-PATH FIX (PR #198)**: the Compose Solo->Skip hand-off (songbook.js) dropped the progression's `seq` (passed only key+mode), so progression-aware inference could never fire on the Skip path - the exact path UAT used. Now Skip carries `progression.slice()` as seq, matching the Save path. | operator UAT 2026-07-10 (post-#197 live) |
| S-KEY-SPELLING | **A note/chord spelled with a SHARP contradicts the degree label the same UI shows (music-theory-coach REJECT).** Building C-F-A# in C major, the third chord shows "A#" under a "bVII" roman-numeral label. Music-theory-coach verdict: over a stated key, spelling is FUNCTION-driven - the bVII is the lowered 7th (B lowered = **Bb**), and "A#" names a raised 6th (#6), the WRONG function. Showing "A#" while labeling it "bVII" is internally self-contradictory (numeral says flat-7, note says sharp-6). Coach REJECTS whenever the UI asserts a key/degree function; sharp spelling is acceptable ONLY in a keyless/chromatic context. This is the FORK-4 canonical-sharp limitation - the fix is key-aware spelling, which is exactly what the stalled linchpin **#98** (`spellScaleKeyAware`/`keyLabel`) delivers. Operator (2026-07-10) AUTHORIZED removing FORK-4 -> key-aware, confirming it is deterministic. **CORE BUILT + PROVEN (PR #198)**: `Circle.spellScaleKeyAware(root,mode)` + `Circle.spellRootInKey(keyRoot,keyMode,noteRoot)` - deterministic letter-per-degree spelling (each of A-G once, accidental to hit each pitch). 14 passing unit tests (`test/key-spelling.test.js`): F major -> Bb (not A#), bVII of C -> Bb, the note name now AGREES with the degree label. REMAINING (the staged wiring = the real FORK-4 removal): (1) route the render paths (chord names, scale note list, fretboard) through the key-aware spellers instead of `spell()`/`spellScale()`; (2) the picker offers SHARP-only key names (A#, D#, G#), and those keys spell to double-sharps under correct theory - so the removal also needs the picker to name flat keys with FLAT letters (Bb not A#). That picker change is a scope/UX call (operator owns). Supersedes the #98 approach with a tested core. | operator UAT 2026-07-10 (music-theory-coach consult + operator go) |
| S-PERSONA-COPY | **G1 (80) SHIPPED (first full USDD arc, red->green)**: Solo modal now speaks the beginner vocabulary budget with outcome-named buttons (Keep + practice / Just practice / Keep building - copy-coach), and beginners get ONE Studio orientation tip (studiofirst notable, pedagogy-coach: action-first, show-once, level-gated) while whynote stays intermediate+. Persona pair proves both sides on pixels; journey state (one-tip-at-a-time) modeled via dismissNotables. | [ux-friction-profiles](ux-friction-profiles-20260710.md), USDD arc |
| S-POSTPROG-CUE | **G2 (75) SHIPPED (USDD arc 2, red->green)**: beginner-gated `postprog` notable fires the moment the progression goes Solo-ready ('Your chords are ready - tap Solo to practice over them'), never a modal; tapping Solo IS the goal so it self-retires (dismiss + slot clear), releasing the one-tip journey slot for the Studio tip - the full beginner journey (cue -> Solo -> modal -> Studio tip) is one pixel-proven scenario. Resolves S-POSTPROG-FLOW's remaining vision half; taste-reviewable on the preview. | ux-friction-profiles G2, USDD arc |
| S-TYPEFILTER-QUIET | **G3 SHIPPED 2026-07-10 (swarm, PR #214, v137)**: library facet chips outline-on (red-first scenario kept); accent fill reserved for primary/mode - D-SELECTED-ACCENT intact app-wide | ux-friction-profiles G3 |
| S-JAM-STARTER | **G4 SHIPPED 2026-09-01 (PR #342, v342-2)**: no-video Studio empty state offers ONE curated starter chip for the current key ('Play a pop jam in C' shape), resolved via the finder's own filterTracks ranking + activate() path (no duplicate loader), hidden once a video exists; red->green scenario jam-starter.json; the 'Find a jam' disclosure stays intact beneath it | ux-friction-profiles G4 -> PR #342 |
| S-SCALE-MEMORY | **G6 (24) SHIPPED 2026-07-10 (board resume, PR #220, v139)**: tapped scale chip persists per track (bt.soloScale.v1, additive, tap-only - the synthetic default select never pins an inference); remembered choice wins only while still an offered chip. | ux-friction-profiles G6 |
| S-FIRSTSESSION-SCENARIO | **CE3 SHIPPED 2026-09-01 (PR #342) - and the app already meets it**: first-session.json committed as the canonical regression, GREEN on first honest run (tour -> jam -> back -> Compose -> starter progression -> chord tap = ChordAudio fired, exactly 5 taps); audio assert wraps the real strum/tone engine, not a proxy | ux-friction-profiles CE3 -> PR #342 |
| S-NOTABLE-PREEMPT-TEARDOWN | **SHIPPED 2026-07-10 (swarm, PR #215, v138)**: renderBanner registry + teardown-on-preempt at the primitive; teardown chosen over stack per the one-tip law (justified + reversible in the PR); 24 notables tests green | PR #215 |
| S-KEYPICKER-FLATNAMES | **RESOLVED as S-KEYPICKER-PREFERRED 2026-07-10 (branch claude/keypicker-preferred-names, v142 - PR pending connector re-auth)**: operator answered IV-2 by directive ("key selector drift - shows sharps only. SSOT") - every key selector now displays preferred key names from the ONE provider (Circle.preferredTonicName): Compose root grid, compact chip, key readout heading, repertoire-form Key dropdown, triads-page chips (on its own branch). Tokens stay canonical (storage/deep-links/transpose). Built FRESH - the cancelled ec74a46 stays untouched; the flat-keys mock branch is superseded and can be deleted. Scenario keypicker-preferred-names.json. | operator UAT 2026-07-10 |
| S-WHYNOTE-SCALE | **G5 (48) SHIPPED 2026-07-10 (board resume, PR #220, v139)**: whynote banner re-derives its copy per selected scale chip (static templates named by chip label, theory-coach-checked, textContent swap on the live element - no re-claim); personas re-proven on pixels with shape-based asserts (/Why .+ works:/ - content-pinned rot fixed a 4th time, now in the persona pair). | in-Claude review of PR #198, 2026-07-10 |
| S-TRIADS-SPELLING | **FIXED 2026-07-10 (branch claude/triads-key-spelling, v140 - PR pending GitHub connector re-auth)**: Triads & Inversions kept its own sharp-only NAMES table from before the FORK-4 retirement, so F major showed "IV = A#" instead of Bb (operator UAT). All displayed chord/note names now route through Circle.noteInKey (theory SSOT, no duplicate speller); tokens (key chips, deep-links, storage) stay canonical-sharp. Regression scenario triads-key-spelling.json (shape assert: no A# anywhere in an F-major cycle). | operator UAT 2026-07-10 |
| S-TRIADS-AUDIBLE | **SHIPPED 2026-07-10 (branch claude/triads-audible, v143)**: inversion chord cards are tap-to-hear buttons - a tap strums the card's exact fullFrets voicing from the ACTIVE profile's open-string frequencies (accurate on every supported instrument by construction; shared ChordAudio engine, keyboard-reachable role=button). S-RN-STYLE folded in: standalone I/IV/V numerals in prose carry the .roman mono+accent family so they never blend into sentences (operator UAT). | operator directive 2026-07-10 |
| S-DELETE-UNDO | **Compose progression delete needs protection + undo (operator UAT 2026-07-10: "too easy to delete a chord... offer 1 (or 2?) levels of undo using simple mechanism tracking previous actions").** Design intent: no confirm() (feedback-debt direction is REMOVING those) - reuse the app's existing undo-toast pattern (Toast.showAction, the setlist item-remove undo) + a small bounded action stack (1-2 levels) for progression deletes. SHIPPED 2026-07-10 (branch claude/delete-undo, v144): first x tap ARMS (red, 1.6s auto-disarm, movement-cancelled taps intact), second deletes; bounded 2-level remove-undo stack on the Clear-undo toast pattern; deletes keep the stack, every other mutation invalidates it via the A3 choke point (keepRemoveUndo param). Unit suite locks "first tap must NOT delete"; prog-delete-undo scenario proves arm -> delete -> toast -> Undo -> restore on pixels. | operator UAT 2026-07-10 |
| S-PROG-REORDER | **PROTOTYPE SHIPPED 2026-07-10 (branch claude/prog-reorder, v145)**: drag-and-drop chord reorder across all 3 strip stages (2D nearest-center targeting so grid6's second row works). Touch = 300ms long-press lifts (early movement = scroll, untouched); mouse = lift on movement. Accent insertion edge marks the drop side; commit splices + full A3 invalidation; trailing click swallowed so a drop never strums. Live-probe finding: the delete-x 44px hit area covers a compact chip's center - drags may START on it (gestures separable: rm tap is movement-cancelled). Touch FEEL is the operator's pass. | operator directive 2026-07-10 |
| S-SETRM-ARM | **SHIPPED 2026-07-11 (#235, v150)**: setlist edit-mode remove handle joins the ONE inline-remove grammar (S-DELETE-UNDO) - quiet at rest, first tap ARMS red (1.6s auto-disarm, gate at the list-item PRIMITIVE), second tap removes, undo banner kept; J2 rest-red retired (red-at-rest reserved for labeled .btn.danger). Red-first scenario setlist-remove-arm + consistency-lint rule + source lock. | operator UAT 2026-07-11 (screenshot) |
| M-COCKPIT-V2 | **VISION + MOCKUPS SHIPPED 2026-07-11 (#236)**: fractal portfolio Cockpit - vision doc (granularity ladder, Postgres 4-table spine, FastAPI+SSE, agent execution backplane / zero-context-switch tenet, competency profiles, dept onboarding) + operator friction profile (USDD persona, signal contract C1-C7) + 3 tactical angles under docs/artifacts/cockpit-v2/ (instruments / tempo / signal), each scenario-verified at 412x915. **PICKED 2026-07-11: remix "C mostly + B timeline" (operator digits) - built as cockpit.html (#240); hub header grammar adopted, label = COCKPIT (#239 + workflows-hub #46 deployed). LZ HANDED OFF: ps-internal packet (ccp handoffs/cockpit-lz-packet-20260711.md) -> spawned autonomous session building FastAPI + schema spine behind AAD (notifies operator directly).** | operator mission 2026-07-11 |
| S-UI-RECONCILE | **SHIPPED 2026-07-11 (audit + 4-lane fix swarm, PRs #230-#233, v146-v149).** Read-only audit ([ui-reconcile-audit-20260710.md](ui-reconcile-audit-20260710.md)): 14 keyed-surface spelling misses + 8 primitive divergences, with a correctly-keyless guard list. Fixes at PRIMITIVES: Lane A song-view/Stage view-local speller (the screenshot bug - chip row/sheet/Stage/transpose/Maximize respell, no-double-transpose guarded) + delete button = danger primitive + .full class seam; Lane B Library facet chips/row badges/form Key dropdown (keyRank order preserved, values stay tokens); Lane C Studio prose/jam query/scale-tip mode/relative-key names (circle.js kept pure); Lane D 9 radii snapped to role tokens + 10 lint rows + the .actions .btn.full seam rule. DEFERRED (registered): chord-chip primitive unification (.chordChips .c vs .bt-st-chordchip - markup change, follow-up), native confirm() dialogs (SETX phase 2), .li-up/.li-dn setlist keys (findings-register rider). | operator UAT 2026-07-10 (2 screenshots) |
| CE-SPAWN-ENV-REPOLOCK | **Friction (infra, diagnosed 2026-07-11): BOTH overnight ps-internal spawn sessions died at repo-attach with zero pushes.** Root cause: fresh-session triggers fired into the Cloud-With-Config environment (env_018ePk6pcuQ3xVYDS2dKausA), whose pre-attached nhruska sources lock the session's owner tier BEFORE its first add_repo - so `add_repo problemsolutions/ps-internal` dies on the cross-owner v1 limit at STEP 0 (triggers trig_014Ey2aRwZ2JzncgUnVkKu9S 09:05Z + trig_01RpyWGkzriwEhDvLyPLcge8 11:42Z, both `run_once_fired`, pushed_at never moved off 2026-07-07). The corrected path (spawn into the Default env, env_011CUMCmi2MSZxFpRc21acMx, no pre-attached repos) needs a permission approval the autonomous surface could not deliver ("permission stream closed" x4, including an AskUserQuestion). Encoded lesson: **cross-owner missions must spawn into an environment with NO pre-attached sources for the other owner tier** - the Default env, or a purpose-made problemsolutions env. Operator surfaced via 🔴 telegram (msg 10); mission packet stays live in ccp handoffs/cockpit-lz-packet-20260711.md. | overnight /goal arc 2026-07-11 |
| S-SETADD-EVIDENT + S-SETADD-KEYSEED | **SHIPPED 2026-07-12 (UAT burst pair, #249 v157 + #250 v158)**: the Library + is a true 3-way setlist picker per the operator's bursts. #249: chordless rows get a GHOST + at the list-item primitive (dashed/faint, reason as aria-label, explaining toast) - an empty add slot never ships. #250: a row with a KNOWN key but no chords gets a LIVE + that seeds the key's tonic chord through the existing record machinery (updateCustomItem / the openEditOrAdd promotion shape - matchKey merge, no duplicate rows) and adds to the set; ghost narrows to keyless+chordless only. Scenarios setadd-keyseed + setadd-evident (fixture-seeded) + regressions green. DEFERRED (vision candidate, operator-gated): setlist-jam-items - chord-less tracks addable AS jam items (Perform learns to play tracks, not just sheets). | operator UAT bursts 2026-07-12 (2 screenshots + "add one chord... be consistent") |
| CE-SPAWN-MIDRUN-DEATH | **Friction (infra, 2026-07-12): fresh-session spawns in the Default env die MID-RUN.** Attempts 3 (trig_013hDfzZgXTWZRfFwhQvUnpy, 13:50Z) and 4 (trig_01RiqP8WimPtyE8WSiaiuZwb, 20:18Z, adopt-mode) both attached ps-internal, pushed a branch + draft PR within ~7 min, then went permanently silent (attempt 4 despite an explicit 15-min push-cadence order; last pushes 13:57Z / 20:34Z). Signature: repo-attach + early pushes succeed, session dies ~10-15 min in - cause not diagnosable from the orchestrating session (no transcript access). 4-attempt cap enforced; operator holds the recovery (own session adopting the claude/cockpit-lz branch - 🔴 telegram msg 12). Encoded lesson: **long autonomous cross-owner missions need a HUMAN-attached session or a purpose-built env until spawned-session stability is proven; the adopt-the-branch guard made attempts cheap and work cumulative.** | ps-internal arc 2026-07-12 |
| CE-WORKTREE-PREFLIGHT | **Friction (agent-side, self-caught 2026-07-11 overnight): the parent's coupled-step commit landed on the LOCAL main checkout** because `git worktree add` for a branch already checked out in the agent's worktree fails silently under `|| true`, and the follow-up `cd` fell through to the main checkout. Nothing bad was pushed - recovered by cherry-pick onto the branch + reset of local main. Encoded lesson (agent-spawn-min-context already mandates it - the miss was skipping it): ALWAYS run `git worktree list` collision pre-flight before any worktree add, and never chain worktree-add + cd + edits with error-suppression. Subsequent chains (g2/g3) ran the pre-flight and reused the agent's own worktree. | overnight arc 2026-07-11, PR #243 process note |
| CE-APP-DROPPED-INPUT | **Friction (surface-side, operator-flagged 2026-07-11): the Claude Android app DROPPED a typed operator message** - his preferences burst ("using metaphors in lyrics... compounded from previous sessions") never arrived and had to be re-sent via screenshot. Cost: one operator round-trip at ~1AM. Not fixable app-side from here; mitigation encoded: treat screenshots-of-typed-text as first-class burst input (already practiced), and the queues.json interview surface remains the durable channel for anything long. | operator burst 2026-07-11 (re-sent) |
| S-TESTSTEPS-EXECUTABLE | **Friction: prescribed testing steps were not intuitive or possible.** The agent's "build C, Bb, F" test steps failed the operator: (a) Bb is non-diatonic to C so it's not offerable from the suggested/In-key palette (had to discover the All view + that Bb renders as A#), and (b) the agent had not traced the Skip path end-to-end, so the test could not have passed anyway (seq was dropped - see S-SOLO-SCALE-DEFAULT fix). Mobile-dev-coach lesson: never prescribe a manual test the agent has not traced as executable ON THE ACTUAL SURFACE and data-path. Encode: verify the code path carries the data before writing the test steps; write steps in the app's own vocabulary (All view, A# not Bb). | operator UAT 2026-07-10 |

## MID (vision known - needs a spec interview, ~15-30 min each; batchable in one sitting)

| # | Candidate | Vision source |
|---|---|---|
| M-AGENT | **AGENT INTERCHANGE (spec drafted 2026-08-17, extended same day)**: coding-agent interaction contract - self-describing exports (bundled AGENTS.md in the skills zip), static `music/agent/` capability manifest + consistency gate, agent round-trip node test, provenance-surfacing import copy, PLUS agent-actionable setups (`?jam=` deep link: progression + key + keyless yt, ephemeral until user Save) and plugin packaging (coach bench + contract + `/music:*` commands installable in CC/Cowork). L0 file-interchange is the floor (offline, no server); L1-L3 degrade to it. Missions A1-A9 sized in the spec; operator gate to promote A1+A2+A4 (then A7) to SHORT. Demo framing: skills absorb the LMS (content, coaching, competency records as portable skill artifacts). Adjacent pointer, separate session: personal-skills second-brain graph | [goal-agent-interaction](goal-agent-interaction-20260817.md) |
| M-0 | **DETERMINISTIC TUTOR (vision captured 2026-07-05)**: skill ladder + progressive disclosure + ear-training/pitch/recognition drills + spaced repetition - ALL no-LLM on shipped machinery; the Tier-2 sitting centerpiece merging #84 + P4/P5 + M-CONSTRUCT/M-PHRASE | [vision-tutor-deterministic](vision-tutor-deterministic-20260705.md) |
| M-1 | Tutor Phase 4 complete: decoupled solo-scale selector over a FIXED progression (relative/parallel demos in place) | [TUTOR-ROADMAP](../../music/TUTOR-ROADMAP.md) Phase 4 (PARTIAL) |
| M-2 | Tutor Phase 5: song-form coaching (AABA, sections -> whole songs) | TUTOR-ROADMAP Phase 5 (PLANNED); operator Q5 2026-07-04: M-CONSTRUCT is a SEPARATE mission (with future-features intel) - P5 stays teaching-focused, M-CONSTRUCT is builder-focused |
| M-11 | M-CONSTRUCT: song construction + section transitions (OWN mission per operator Q5) - kickoff includes an INTEL pass on possible future features | [vision-ear-first](vision-ear-first-20260704.md) |
| M-3 | Strum-engine revival (draft PR #88: Karplus-Strong + humanized hand) | Existing draft PR - needs vision refresh + quality gate |
| M-4 | Music-app regression suite in CI (Playwright journeys J1-J4 + T-plans as automation) | Sprint-1 harness + test plan exist; needs CI-shape decision (box OOM rule -> CI-dispatched) |
| M-5 | buildAdapter extraction (inline HTML -> shared/chord-pack-adapter.js + real tests) - before next instrument profile or HSR Lens | [analysis A3](analysis-refactor-enhance-20260704.md) |
| M-6 | Client-storage strategy + schema inventory (issues #76 + #77) - the strategic layer above S-SAVE-TRUTH | gh #76/#77 + analysis A1 context |
| M-7 | ~~Jam picker~~ CLOSED WON'T-DO (2026-07-04): the entry point was DELIBERATELY removed by operator interview 3 days earlier (commit 8cf0647, never registered - now D-HERO-REMOVED); re-adding = re-litigation. OPEN FORK for Tier-2 sitting: a lightweight STATIC jam-now affordance (no show/hide-on-filter) - operator call | agent stop + git archaeology |
| M-8 | SUGG table -> shared module + spelling-agreement test; modeSwitch selected-state language unify (taste pick at interview) | analysis A7 + B5 |
| M-9 | mount() god-function incremental extraction - FOLD INTO M3, never big-bang | analysis A2 |
| M-10 | M-PERFORM: true play/perform songbook experience (full sheet layout, auto-scroll?, stage mode?, set flow) - VISION INTERVIEW required | [uat-walkthrough-20260704.md](uat-walkthrough-20260704.md) U1 - first shared-walkthrough finding |
| M-12 | **S-SDD-TEMPLATES SHIPPED 2026-07-10 (swarm worktree agent, PR #216)**: SongTemplates runtime mining (sections -> roman patterns, ranked + song-cited) + canonical families, 16 tests; reuses Repertoire.deriveKey + Circle.romanFor. M-13's template input is LIVE | [sdd-vision](sdd-vision-20260710.md) + PR #216 |
| M-13 | **M-SONG-WIZARD SHIPPED 2026-07-11 (overnight /goal arc, 4 PRs)**: LZ #241 (v151, section buffer in Compose assembles multi-section songs) -> g1 #242 (v152, template-suggested sections per label - M-12 payoff - + suggested/adjacent-section compatibility cues + catalog-cited recommendations, folding the operator's UAT burst) -> g2 #244 (v154, buffer persists across reloads via additive songbook.builderBuffer.v1 + up/dn section reorder) -> g3 #245 (v155, suggestions re-rank by the competency profile + preferences with a "your style"/"easy fit"/"adds variety" why-cue, accepts record evidence back - the compounding personalization loop closed). Every PR: red-first scenario + full ship chain under the overnight delegation. REMAINING (new goalposts, operator-taste-gated): drag-to-reorder in the buffer (S-PROG-REORDER pattern), tutor-style guided entry polish. | operator UAT bursts 2026-07-11 + [guided-and-free-vision](guided-and-free-vision-20260710.md) |
| M-14 | M-SKILL-PROFILE: **CORE SHIPPED as M-COMPETENCY 2026-07-11 (#243, v153)** - per-skill competency profiles (5 frameworks: fretboard/uke/guitar/composition/lyrics), evidence-driven leveling with diminishing returns, portable skill-competency-profile/v1 export/import (higher-wins merge, preferences union), Skills panel in Settings, evidence hooks from song building; g3 #245 wired the consume side (personalized suggestions). PRIVACY INVARIANT: public repo ships generic frameworks only - personal levels/preferences live in localStorage + ccp (profiles/skills/, seeds in ccp #20/#21). REMAINING: pedagogy-coach next-best-step surfacing + mastery path as USDD persona-state transitions (target profile - current profile = the practice queue). | [guided-and-free-vision](guided-and-free-vision-20260710.md) sections 3-4 + #243/#245 |

## LONG -> promoted by the 2026-07-04 vision capture

| # | Candidate | State |
|---|---|---|
| M-EAR | Scale/mode AUDITION - hear it, hum it, A/B modes ("music is heard not seen") | Vision CAPTURED ([vision-ear-first](vision-ear-first-20260704.md)); 5 async interview Qs pending; strum-engine #88 revival becomes its foundation |
| M-SONG-ANCHORS | Known-songs-per-mode memory anchors + YT links | same vision doc |
| M-PHRASE | Solo phrasing depth (hang notes, tension/release) | same vision doc; extends shipped mentor layer |
| (merge) | M-CONSTRUCT folds into/ sharpens Tutor Phase 5 (M-2) | operator Q5 pending |

## LONG (ideas - need a vision interview before anything)

- KNOWLEDGE SPHERE for competencies (operator 2026-08-17): node-graph viewer over the competency map (skills as spheres, evidence as edges) + explore/exploit analytics - "which competencies have served me best -> adjacent ways to explore/exploit". Composes M-AGENT profile docs as the data source; adjacent to the personal-skills second-brain graph (separate session, operator-flagged)
- Practice-tracking / streaks (accountability loop per tutor north star)
- Setlist sharing / export for bandmates
- More instrument profiles + per-instrument voicing depth (operator signal: "huge toolset for guitarists AND OTHERS")
- Audio-input features beyond tuner (chord recognition?) - speculative (NOTE: distinct from M-EAR audio-OUTPUT, which is captured)
- Compose UX redesign: tabs + Related Keys lens (gh #44)
- Setlist touch drag-to-reorder (gh #92)
- SDD recursion: album composition (sequencing, key/tempo/energy arcs across a set) - parked until song-level SDD proves out ([sdd-vision](sdd-vision-20260710.md))
- SDD recursion: release cadence / streaming-strategy optimization loops - external-data spike + vision interview first (same doc)
- ORG COCKPIT: single-operator organization glass (departments as lanes, skills inventory as bench, deliverables-with-evidence) - vision CAPTURED ([org-cockpit-vision](org-cockpit-vision-20260710.md)), IV-4 interview set on the Deck; cockpit boundary = actuators (verticals/industries out of scope)
- Kalimba instrument profile: operator plays one, and its geometry is a teaching gift - alternating tine layout means THREE ADJACENT TINES ARE ALWAYS A TRIAD (operator, 2026-07-10). A profile + triad-adjacency guidance would make it the easiest chord-theory instrument in the app

## Operating rules

0. **Atomic tier plan active:** [atomic-queue-plan-20260704.md](atomic-queue-plan-20260704.md) - Tier-0 relay-launches autonomously (5-builder cap); Tier-1 quick picks asked as one round; Tier-2 = one batched sitting. This section governs launch order until superseded.

1. SHORT items launch autonomously when their trigger fires and quota is available - INFO ping, no gate re-ask (they were gated at spec time).
2. MID -> SHORT requires one operator interview sitting (batch several per sitting - that is the front-loading).
3. LONG -> MID requires a vision interview (fuzzier, longer).
4. Reviews batch: agent stacks review-ready items with adversarial pre-passes done, operator sweeps them in one window (taste/vision judgment only).
5. Scorecard tracks: dormant-quota-hours (target ~0 while SHORT non-empty), SHORT-queue depth (target >=2), interview-batch efficiency (specs gated per sitting).
