# SBS plan - PR merge run for nhruska/nhruska.github.io

> Step-by-step procedure for clearing the 6 open PRs as of 2026-09-26 02:08 UTC. Produced per the claude-config pr-merge protocol. Every step that merges or closes is Nik's act - the repo CLAUDE.md makes Nik the only merger, which overrides the `auto` merge policy in the projects table.

## Context (2026-09-26 02:08 UTC)

`main` is at `c46a08e` (#352, `music-v352-2`). All 6 open PRs are drafts - this repo opens every PR as a draft, so "draft" is not a WIP signal here, and marking a PR ready is Nik's promotion gate. Nothing is queued until it's marked ready.

All four music-touching branches conflict with `main`, and each one conflicts in the same four files: `music/play/index.html`, `music/play/triad-inversions.html`, `music/shared/build-stamp.js`, `music/sw.js`. That's the SW cache-bump pair plus the asset `?v=` stamps. It's mechanical, not a logic conflict, but merging one PR re-dirties the next one's quartet. So each branch gets resolved **right before** its own merge, one at a time, never in parallel. Because the branches conflict, no CI has run on their current SHAs. Don't trust any existing check mark until the branch has been resolved and CI re-run.

| PR | Base | Behind / ahead main | Size | Conflict |
|---|---|---|---|---|
| [#355](https://github.com/nhruska/nhruska.github.io/pull/355) Math app v1 | main | 2 / 38 | 60 files, +7444/-261 | cache quartet |
| [#351](https://github.com/nhruska/nhruska.github.io/pull/351) Musician Profile (review-only) | main | 15 / 7 | 50 files, +4538/-336 | cache quartet |
| [#350](https://github.com/nhruska/nhruska.github.io/pull/350) Setlist scroll fix | main | 17 / 3 | 9 files, +133/-60 | cache quartet |
| [#348](https://github.com/nhruska/nhruska.github.io/pull/348) Guided Tune | main | 18 / 10 | 20 files, +2344/-206 | cache quartet |
| [#346](https://github.com/nhruska/nhruska.github.io/pull/346) Thought-Ware claims | propose/methodology-v0.1 | stacked | 22 files | n/a |
| [#347](https://github.com/nhruska/nhruska.github.io/pull/347) Wiki lifecycle | propose/public-claims-calibration | stacked on #346 | 27 files | n/a |

## Order of operations

| # | PR | Blocker | Effort |
|---|---|---|---|
| 1 | #350 Setlist scroll | Superseded by #353 (already on main) - close | 5min |
| 2 | #355 Math app v1 | Cache quartet + a music theme-regression check (palette moved to `shared/theme.js`) | 30min |
| 3 | #348 Guided Tune | Taste gate: the UAT batch 3 feel verdict is still open, plus overlap with #352's dock padding | 30min + UAT |
| 4 | #351 Musician Profile | Title says review-only. Scope decision needed, then a dedicated review session | 2-4h |

## Step-by-step per item

### Step 1 - PR #350 (setlist scroll)

**Status quo:** The branch adds `#setBody>*{flex-shrink:0;}`. The same fix, for the same root cause (flex rows with `overflow:hidden` shrinking to their automatic min-height of 0), landed on main as [#353](https://github.com/nhruska/nhruska.github.io/commit/4e1db97) (`4e1db97`). #350 would only add a comment block, its scenario file `test/pw/scenarios/setlist-scroll.json`, and screenshots.

**Action:** Nik closes #350 as superseded by #353. Optional salvage: cherry-pick `test/pw/scenarios/setlist-scroll.json` onto a fresh branch, since main has the fix but not the regression gate. Recommended, and it's about 5 minutes.

**V&V:** `git grep -n "flex-shrink" origin/main -- music/shared/songbook.css` shows the fix on main. If the scenario is salvaged, it must pass on main and go red with the rule removed.

**Exit criteria:** #350 state = closed, with a comment naming #353. If salvaged, a scenario PR is open.

### Step 2 - PR #355 (Math app v1)

**Status quo:** This is the freshest branch (2 behind). It adds `/math/` (a PWA with its own `sw.js` and version) and lists 2 apps on the landing page. It also touches music: the jewel palette moves out of `music/play/index.html` into `music/shared/theme.js` (+29 lines), `music/sw.js` changes by 20 lines, and it edits `songbook.css`, three music wiki pages, `.github/workflows/pr-preview.yml` and `CLAUDE.md`. It also extends `scripts/check-cache-bump.sh` with `check_math()`.

**Action (agent):**
1. `git merge origin/main` into the branch (merge commit - never rebase a pushed PR branch).
2. Resolve the quartet: take main's content and re-stamp `CACHE` and `VERSION` to the next counter, `music-v355-4`, with `UPDATED_ISO` set to now. Then restamp every `?v=`.
3. Run `node test/run-all.js` and `bash scripts/check-cache-bump.sh`.
4. Render-verify `music/play/` at Pixel 5 and 1440, in both dark and light. Confirm the accent palette looks identical to main. The theme extraction is the only music regression surface in this PR.
5. Push, watch CI to green, and post a PR comment.

**Action (Nik):** Mark ready, then merge.

**V&V:** Suite 0 failed. Cache gate OK. Zero app console errors on both `/music/play/` and `/math/`. Before and after palette screenshots committed under `docs/artifacts/math-v1/`.

**Exit criteria:** #355 `mergedAt` is set, and the live `/math/` and `/music/play/` builds show stamp `v355-4`.

### Step 3 - PR #348 (Guided Tune)

**Status quo:** Guided Tune replaces the strobe with a runway, a string-by-string loop, drone cancellation (`cancelDrone`, f0+5f0), and a `?tunerlab=1` slider lab. The last push was UAT batch 3 (`fd07cae`, `v348-7`), made while waiting on Nik's guitar feel report. `tuner.js` has +526/-91 lines. Main's #352 separately padded `.tunerWrap` so the PIP dock stops covering the tuner strings. That's textually clean against #348's layout rewrite, but it's a semantic overlap. Known gap: the real-mic path with the tone playing through the phone speaker has only synthetic evidence.

**Gate (Nik, taste):** Is the Guided Tune feel ready to ship, or is there another UAT batch? Nothing below runs until this is answered.

**Action (agent, after the gate and after #355 merges):**
1. Merge `origin/main` and resolve the quartet to `music-v348-8`. `v348` has never shipped on main, so the lower number is still unique.
2. `node test/run-all.js`, the cache gate, and `test/pw/scenarios/tune-guided.json`.
3. Render the Tune tab with the PIP dock open at 412x915. Assert the dock doesn't cover the runway or the string row, using `elementFromPoint` at each string button.

**Action (Nik):** Mark ready, then merge.

**Exit criteria:** `mergedAt` is set and the live stamp is `v348-8`.

### Step 4 - PR #351 (Musician Profile + VNext)

**Status quo:** The title marks it review-only. It's the largest music change: a portable profile, a competency model, guidance level, capability manifest, and a `plugin/music-coach/` coach contract. It's 15 behind main. `backup.js` changes without a `SCHEMA_VERSION` or `MIGRATIONS` bump, so the review has to confirm every stored-data change is additive, per music/CLAUDE.md.

**Gate (Nik, scope):** Is it still review-only, or a ship candidate? If it's review-only, leave it open with a status comment and stop here.

**Action (if ship candidate, in a dedicated session):** Merge main and resolve to `music-v351-7`. Audit `backup.js` for additivity. Run the suite plus the profile round-trip tests. Run an adversarial review volley (codex Sol, since this is data-model and migration risk). Render-verify Settings > Musician profile.

**Exit criteria:** Either `mergedAt` is set, or a status comment is posted and the PR stays deferred.

## Cross-cutting concerns

- **Conflict default:** For the cache quartet, always take main's content, then re-stamp to the branch's own `music-v<PR#>-<next>`. Never hand-merge the `?v=` lines. Restamp them all from the resolved version.
- **Sequential only:** Each merge changes main's quartet, so the next branch gets resolved after the previous merge lands.
- **CI:** Conflicting PRs ran no workflows. Re-run and watch CI after each resolution push before marking ready.
- **Merge authority:** Nik only. Agent-side work stops at "resolved, green, commented".

## Open / Deferred

| PR | State | Reason |
|---|---|---|
| [#346](https://github.com/nhruska/nhruska.github.io/pull/346) docs: calibrate Thought-Ware public claims | DRAFT, stacked | Not the music app. Its base `propose/methodology-v0.1` has **no open PR** (23 behind / 2 ahead of main), so it has nowhere to land. Decision: open a PR for the base branch, retarget #346 to main, or close the stack. |
| [#347](https://github.com/nhruska/nhruska.github.io/pull/347) docs: enforce engineering-wiki lifecycle integrity | DRAFT, stacked on #346 | Same stack. It can only land after #346, which merges first by stack order. |
| #351 | DRAFT | Deferred unless Nik flips its scope (Step 4 gate). |

All 6 drafts appear only here or behind an explicit Nik gate. None is queued for merge without being marked ready.

## Resumption pointer

Open this file and start at Step 1. First re-run the open-PR list and `git fetch origin main` to catch new PRs or a moved main. If main moved, redo the quartet resolution for whichever step is next.
