# session handoff notes — local only, not part of the deployed site
_handoff/

# os / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/

# python build cruft (scripts/generate.py)
__pycache__/
*.pyc

# node deps for the local render-verify harness only (no-build site; not a real dep)
node_modules/
package.json
package-lock.json

# session files = local continuity, never commit to this PUBLIC repo (keep the rest of .claude visible to signal CC usage)
.claude/sessions/
