# Practice Studio

[Wiki](../index.md) > systems > Practice Studio

## Purpose

The solo-teaching hub: given key + mode (+ optional backing track), show the scale to solo over on the real fretboard, the chords in key (tap to hear), and the "why" - without page reloads.

## Architecture [STABLE]

Entry: `Tracks.openStudio(t)` (tracks.js) - t carries .key, .mode, optional .yt. Three entry paths funnel here: the Tracks list activate(), Compose's Solo entry (`#soloBackingBtn` in `#chordCtrlRow`), AND the song-view Solo (`#soloOverBtn` in `.practiceRow`) - the latter two via the songbook.js bridge (the payload rehydrates curated .yt by trackKey before render; F28+F29 UI-std UAT relabeled the entry "Solo" and moved it into the shared controls row beside the In-key|All toggle, same gate/bridge unchanged). Dismiss (F32 UI-std UAT): the app's standard Back affordance (`.bt-st-back`, composes `.iconBtn` - see [ux-philosophy/component-conventions.md](../ux-philosophy/component-conventions.md) Buttons table), not the retired `.bt-st-x` "close" pill; same `NavHistory.dismiss()`/`closePlayer()` wiring.

Layout: fixed stage (pinned video iframe or search card + title/BPM/genre + URL curation editor) over a scrollable body (solo scale fretboard -> scale chips -> chords-in-key -> buildWhy).

## Scale-first philosophy [STABLE]

The fretboard is the hero: every note that sounds right in this key/mode, on THIS instrument's geometry, position-walkable up the neck. Chords support the harmony; the pattern is the guide (see [expertise-adaptive-display](../ux-philosophy/expertise-adaptive-display.md)).

## studioTheory bundle [STABLE]

tracks.js (~196-296): computes { key, scaleMode, label, notes, degrees, pcs, rootPc, chords } for a key+mode - the one bundle feeding the notes line, fretboard (via KeyExplorer.renderScale with a pc->name map so list==fretboard), and chords-in-key. Mode coarsening (normMode/resolveScaleMode) maps free-text modes onto the supported set.

## Solo scale chips (S-BLUES) [STABLE]

One chip row `.bt-st-scalechips` in the Solo section: [Mode label | Pent major | Pent minor | Blues]. Default = the mode scale (open behavior unchanged). A tap re-derives ONLY the solo bundle via the pure `Tracks.soloBundle(key, mode, scaleId)` ('mode' delegates to studioTheory; others go through Circle.SOLO_SCALES). Harmonization surfaces are untouched by selection - enforced by the isolation test (chords identical before/after). Per-open state only (no persistence). Framing line per selection is a static template (A9 discipline). See [theory-engine/solo-scales.md](../theory-engine/solo-scales.md).

## buildWhy - the read-only wheel (S2/FORK-3) [STABLE]

The "why these notes" box renders scale degrees + notes + a READ-ONLY tinted circle-of-fifths (relative key highlighted, IV/V neighbors dimmer; tracks.js ~489-516). Deliberately non-interactive: the Studio is for practicing, not key selection - no false affordance. Computed once per Studio open (cached on first toggle).

## whynote JIT banner [STABLE]

One-shot relative/parallel "why" teaching notable at Studio entry (whynoteText/whynoteBanner near studioTheory; claims 'whynote' via Notables; A9 static templates keyed on the mode label). Dismiss = forever.

## Curation loop [STABLE]

Tracks without a curated video show the search card + paste editor; curated URLs persist in the music.trackUrls overlay keyed by trackKey. The candidates queue (candidates.js) seeds pre-researched suggestions. Finder-tab retirement is part of M3 (sprint 2) - the Studio remains reachable from the merged repertoire.

## Chord-tone targeting (M-GUIDE W3a, D-TARGET, amended by the P5 fold) [STABLE]

Chords-in-key doubles as the target surface: tapping a chord (in addition to playing it) marks ALL of that chord's tones, precedence root > chord > blue > scale - `Tracks.targetTones(scalePcs, scaleRootPc, chordName)` (pure pc arithmetic, tracks.js) computes `Circle.chordTones(chord)` against the scale. Tones INSIDE the scale render filled (`kx-root`/`kx-chord`); tones OUTSIDE the scale render as hollow GHOST dots (`kx-ghost`) at their correct fret position - the original "intersection-only, no ghost dot" deferral was killed by a P5 seasoned-player adversarial pass (2026-07-05): for A7 over A blues, C# (the major 3rd) is the money note, and hiding it taught the exact habit the feature exists to break. `targetTones()`'s return shape is `{ byPc, rubPc, ghostPcs }` - `ghostPcs` carries the out-of-scale set. A dominant-quality target (has both the major 3rd and the b7) also marks a "rub" note (chordRootPc+3, dashed ring) when it's in scale - e.g. Eb over C7 in C blues, nothing over a plain IV7 whose rub candidate falls outside the scale. `Tracks.defaultTones(bundle)` is the separate ALWAYS-ON mark: the blues solo scale's b5, independent of any active target.

`Diagram.scale(opts)` renders the classes via `opts.tones = { byPc: {pc: class}, rubPc, ghostPcs }` (diagram.js) - EXTEND not overlay: absent `opts.tones` (or an empty/absent `ghostPcs`) renders byte-identical to the pre-targeting default (SHA-256 locked in diagram.dom.test.js). Ghost dots reuse the SAME per-string fret-window math as the in-scale dots (`scalePlan`'s new `ghostsOn(s, wantedPcs)` sibling to `notesOn(s)`), just for pcs the scale doesn't contain - fill:none, `--kx-ghost` stroke + text ink, no dash (the rub's dashed ring stays a distinct signal). `KeyExplorer.renderScale` forwards `opts.tones` through to `pack.scaleDiagram` as its 6th positional arg (the ONE adapter implementation in play/index.html); the returned `boxWrap` carries `setTones(tones)` to re-mark WITHOUT resetting the position-walk (a toggle should never reset `startFret`). A chip switch (Mode/Pent major/Pent minor/Blues) re-applies the same active target (including its ghosts) against the new bundle's pcs.

A static caption renders under the fretboard while a target is active ("Showing C7 inside A blues - accent = chord root, filled = chord tones, hollow = chord tone outside the scale.") - interpolates only already-rendered labels (A9), never a theory computation of its own.

**U3 fix (operator UAT 2026-07-04):** `kx-chord`/`kx-blue` note text used to share the root dot's `--on-accent` ink; tracks.css now defines per-class `--kx-chord-ink`/`--kx-blue-ink` (both themes) so the text stays readable against the deliberately-darkened light-theme fills - see [decisions.md](../decisions.md) if this needs re-litigating.

**U2 fix (operator UAT 2026-07-04):** the position-pager (`key-explorer.js` `.scalePosCtrl`/`.scalePosBtn`) is now an explicit 44px box (was 42px, below the app's GRIP touch floor) with a less extreme disabled-state fade, so back/forward read as equal-size in both states - override lives in tracks.css (loads after songbook.css's base rule).

## Named box positions (S-BLUES-BOXES, D-BLUES-BOXES) [STABLE]

When the active solo scale is pentMajor/pentMinor/blues, the position-pager (above) snaps its back/forward walk to the 5 named box starts computed by `KeyExplorer.boxes(rootPc, scaleId, openPcs)` (key-explorer.js) instead of the fixed 0/5/10 step - 7-note mode scales (ionian/aeolian/dorian/mixolydian) keep the classic fixed walk unchanged. `Tracks.boxScaleIdFor(scaleId, th.scaleMode)` decides eligibility per the currently active chip: an explicit pentMajor/pentMinor/blues chip is always box-eligible; the 'mode' chip is box-eligible ONLY when the track's own key IS the M-GUIDE W2 Blues key (its scale already IS the SOLO_SCALES.blues set). `renderScale`'s `opts.boxScaleId` carries this through; it derives the tuning's open pitch classes from the pack's own `meta.stringNames` via `openPcsFromPack` - no separate profile plumbing needed by the caller, and it degrades silently to the classic walk if that metadata is missing.

A `.scaleBoxChip` element (sibling of `.scalePosCtrl`, tracks.css) shows the current box's label - e.g. "Box 1 - root on 6th string, fret 5" - exactly when the pager's current position lands on a box start; hidden otherwise (an unaligned position, most commonly the initial open view, shows no chip). See [decisions.md](../decisions.md) D-BLUES-BOXES for the box-math contract (CAGED-derived windows, pentMajor/blues sharing rules, wrap-fold) and [theory-engine/solo-scales.md](../theory-engine/solo-scales.md) for the full write-up.

## Guidance cards - SoloGuide (M-GUIDE W3a, D-CARDS-STATIC) [STABLE]

A `Guide` toggle (mirrors `.bt-st-why-toggle`/`.bt-st-why` verbatim - composed, not a new chip variant) sits under the framing line, collapsed by default, re-deriving on every scale-chip select. Content comes from a NEW standalone module, `music/shared/solo-guide.js` (`window.SoloGuide`), loaded before both songbook.js and tracks.js so Compose's solo chips (W3b) can call the identical `framing()` without depending on tracks.js:

- `SoloGuide.framing(scaleId, family)` - MOVED verbatim from tracks.js's old `soloScaleFraming` (identical behavior).
- `SoloGuide.cards` - the raw 7-key x 5-block table (`ionian, aeolian, dorian, mixolydian, pentMajor, pentMinor, blues`), read-only.
- `SoloGuide.card(scaleKey, notes)` - interpolates `{i}` degree-index placeholders against the caller's current note-name array; unknown key -> null, no DOM, no throw.

Content is curated static prose only (A9 discipline, zero theory derivation) - see [decisions.md](../decisions.md) SOLO-BOUNDARY. Card copy was amended twice: the section-8B professor fold (2026-07-04), then a P5 seasoned-player adversarial pass (2026-07-05, [decisions.md](../decisions.md) P5-FOLD) that rewrote most blocks toward chord-relative advice ("target the CURRENT chord," not just the home chord) and fixed one factual error (`pentMajor.shapes`/`framing()`: the relative minor pentatonic is a minor 3rd - THREE frets - below, not two).

## Scale-audition toggle (M-EAR wave 1, D-EAR-1) [STABLE]

"Music is heard, not seen" (vision-ear-first-20260704.md) - a play/stop toggle (`.iconBtn.soundToggle`, tracks.js `wireSoundToggle`/`stopStudioSound` region inside `openStudio`) sits on the Solo-over-it label row. Tapping it hums the CURRENTLY ACTIVE solo bundle (`curBundle.pcs` - whichever scale-chip is selected, S-BLUES) via `Sound.playScale()` (new `shared/sound.js`, the zero-dependency Web Audio provider - see its own header for the noteHz/playScale contract and the `voice()` swap seam). The notes line AND a NEW degrees line under it (`.bt-st-degrees`, mirroring Compose's `.keySoloDegrees` - same primitive, both surfaces) are per-note token spans (`data-i`); `onNote(i)` bounces a `.sounding` marker (`--sound-bg`/`--sound-line`, a THIRD token pair genuinely distinct from SELECTION and GUIDANCE - see [ssot-registry.md](ssot-registry.md)'s Element Consistency Law) across them in sync with the audio. Stops on: second tap, and Studio close (`closePlayer()`, shared with the plain video player, calls `Sound.stopAll()`). Only one playback app-wide - `Sound.playScale()` itself silences whatever the Compose key-preview (see [compose-key-system.md](compose-key-system.md)) was sounding, and vice versa. **Amended by M-EAR wave 1.5 (D-EAR-1.5, next section):** a scale-chip switch WHILE playing no longer stops - it retargets the live loop instead.

## Seamless mode-switch, fretboard sounding lights, full-neck view (M-EAR wave 1.5, D-EAR-1.5) [STABLE]

Operator audition-round UAT (docs/plans/uat-walkthrough-20260704.md U11-U13), minutes after M-EAR wave 1 shipped: (U11) switching the scale-chip mid-audition should keep playing for a live A/B compare, (U12) the sounding note should light on the fretboard too, not just the notes/degrees text lines, (U13) an optional full-neck view instead of the windowed pager pieces.

**U11 - `Sound.playScale()` gains `handle.retarget(newPcs)`** (`shared/sound.js`): queues the new pc sequence; the swap applies at the NEXT `step()` tick, never immediately - the note already in flight (scheduled synchronously via `schedulePass`) finishes naturally, no click/gap. The tick after it restarts the ascent at index 0 of `newPcs` (not wherever the old sequence had reached), so a differing note count (7-note mode -> 5-note pentatonic) resolves cleanly. `retarget()` is a no-op if `newPcs` is empty/unresolvable, or after `stop()`. `tracks.js`'s scale-chip `select()` (`wireScaleChips` region, `openStudio`) now branches on `studioSound` truthy: playing -> update `curBundle`/`curScaleId`/the fretboard FIRST, THEN call `studioSound.retarget(bundle.pcs)`; not playing -> the old `stopStudioSound()` reset, unchanged. The toggle's `onNote` callback reads `curBundle.pcs.length`/`curBundle.pcs[idx]` LIVE (not a value captured at play-start) so the marker + fretboard light always match whichever scale is CURRENTLY sounding, across any number of retargets.

**U12 - fretboard sounding highlight**: `diagram.js`'s `scale()` now stamps `data-pc="<pc>"` on EVERY rendered dot (root/scale/chord/blue alike), regardless of whether `opts.tones` is set - a deliberate, reviewed change to the tones-absent SHA-256 lock in `diagram.dom.test.js` (the `kx-sounding` CLASS itself is still never baked into the render - it's added later via JS `classList`, so that half of the byte-identical contract holds). `key-explorer.js`'s `renderScale()` returns `boxWrap.setSounding(pc)`: a class-swap pass over ALREADY-RENDERED dots via `diagBox.querySelectorAll('[data-pc="pc"]')` - mirrors `setTones()`'s position-preserving precedent, never a full re-render. Lights ALL octaves/strings sharing that pc at once (correct: a scale can repeat a pc across the neck). Re-applies itself automatically inside `renderBox()` so the mark survives a position walk or a `setTones()` call instead of vanishing. `tracks.js`'s `clearSoundMarks()`/`markSoundingNote(i, pc)` (the same functions that already bounce the note/degree token `.sounding` marker) now ALSO call `scaleBoxWrap.setSounding(pc)` / `setSounding(null)`, reading `scaleBoxWrap` LIVE so a chip switch or the Window|Full-neck toggle (which both replace it) is always the correct target. Style: `.kx-sounding` (tracks.css EOF) reuses the SAME `--sound-bg`/`--sound-line` pair the note/degree pill already uses (Element Consistency Law - one primitive, two surfaces) with `!important` (the one sanctioned exception: it's the only kx-* class applied post-render via JS, every other kx-* class is inline-styled by diagram.js at render time).

**U13 - Window|Full-neck view toggle**: a `.viewToggle` segmented control (songbook.css's existing, previously-UNUSED reserved primitive - `ui-primitives.md`/`component-conventions.md`'s documented segmented-control convention - so this shipped with ZERO new CSS for the toggle itself) sits directly above the fretboard. `key-explorer.js`'s `renderScale()` gains `opts.noPosCtrl`: suppresses the back/forward position-control UI (and any box-chip) even when the pack `supportsStart`, WITHOUT dropping the 6-arg `pack.scaleDiagram` call shape (names/tones still forward) - full-neck mode still needs a proper render, it just has nothing to walk. `KeyExplorer.POS_CAP` (14) is now exported so callers can request the full span without duplicating the constant; full mode renders `frets: POS_CAP, noPosCtrl: true` (span 0-14, geometry-verified 399px - `diagram.dom.test.js`'s F=14 width test), regardless of instrument (even 4-string default-12-fret packs get 0-14 in full mode, per the spec). Box-snap (S-BLUES-BOXES) is pager-only, so omitting `boxScaleId` in the full-mode branch is sufficient to keep it window-mode-exclusive without a separate flag. `tracks.js` centralizes ALL fretboard rendering (the initial mode render, every chip switch, AND the toggle) through one `renderFretboard(bundle, scaleId)` choke point, keyed off Studio-scoped `fretView` ('window'|'full', additive `music.fretview.v1` localStorage key, defensive read/write, default 'window' - registered in `data-model.md`'s inventory).

## Landscape split refine (M-GUIDE W3a, D-LANDSCAPE-FLEX) [STABLE]

The existing coarse-pointer landscape two-pane split (`.bt-studio{flex-direction:row}`, gated on `(orientation:landscape) and (max-height:600px) and (pointer:coarse)`) tightened to fit the Guide card + target caption's added content: stage pane 48% -> 44%, `.bt-st-body` padding `12px 12px 18px` -> `10px 12px`, gap `12px` -> `10px`.

## Explore: "Find a jam" panel (M-TRACKLIB wave 1, D-TRACKLIB-1) [STABLE]

"Enumerate genres per mode linked to corresponding search terms to explore and find my faves to pull back into the library" (vision-ear-first-20260704.md M-TRACKLIB) - a `Find a jam` disclosure toggle (composes `.bt-st-why-toggle`/`.bt-st-why` verbatim, same collapsed-by-default pattern as Guide) sits at the end of the Solo section, right after the Legend (M-EAR wave 1.6, below - the target caption it used to sit after is gone). `renderJamPanel(scaleId)` (tracks.js `openStudio` region) resolves `scaleKeyFor(scaleId, th.scaleMode)` into the new `shared/jam-queries.js` (`window.JamQueries` - pure `genresFor(scaleId)`/`feels()`/`jamQuery(key, scaleId, genre, feel)`, keyed on the SAME 7-scale vocabulary solo-guide.js's Guide card uses) to render a genre chip row + a feel chip row (both compose the shared `.chip` primitive - accent-fill `.on`, no new chip variant - per the Element Consistency Law), the generated query as a caption, and a "Search YouTube" external link (leave-app glyph, new tab). Called on Studio open (`'mode'`) and re-derived on every scale-chip `select()` call, alongside `renderGuide()` - the query is KEY-AWARE and scale-context-reactive LIVE (switching the mode/pentMajor/pentMinor/blues chip updates the genre list + query), which is scale-context reactivity, NOT the forbidden filter-driven show/hide-on-filter [decisions.md](../decisions.md) D-HERO-REMOVED retired - the panel's own visibility never changes on a chip tap, only its content.

`jamGenre` resets to the new scale's first genre whenever the carried-over selection isn't in that scale's own list (each scale's genre list is curated separately - e.g. dorian offers funk/latin rock/modal jazz/blues rock, blues offers slow/shuffle/quick-change/minor blues as separate genre entries rather than a 4th shuffle-vs-straight axis); `jamFeel` (slow ~60-80 / mid ~90-110 / up 120+) persists across a chip switch.

"Add to library" (`.bt-st-editlink`, same button class the "Or edit song details" affordance already uses) reuses the EXISTING `opts.onEditRequest` -> songbook.js `openEditOrAdd` seam: an object with no `.id` always takes the prefilled-CREATE branch. Key + mode forward correctly today (mode reverse-mapped from the Circle-internal scaleMode word - ionian/aeolian -> major/minor, dorian/mixolydian pass through, blues -> the same pre-existing `normFormMode` default-to-major gap the "Or edit song details" button already has for a blues-keyed track). Genre does NOT yet forward through that seam - `openEditOrAdd`'s item construction doesn't copy a `.genre` field onto the form item, and `repertoire-form.js` has no bpm field at all - named follow-up (one line in `openEditOrAdd`), not a blocker, since the picked genre/feel is already visible in the panel's own query caption right beside the Add button.

## Tempo control, sounding-lights harden, the Legend primitive, accent-derived palette (M-EAR wave 1.6, D-EAR-1.6) [STABLE]

Operator audition-round-2 UAT (docs/plans/uat-walkthrough-20260704.md U14-U20), minutes after the wave 1.5 shipped: (U14) "needs faster tempo. add a slider for slow/med/fast control." (U15) "I don't see the frets lighting up." (U16) "the legend below needs specific color dots listed with description." (U20) "a choice of colors of notes on the fretboard as they play... dark mode + accent colors... complementary and/or adjacent tones... yielding to you" - folded into this same wave per the capture note ("same surfaces"). U21 (the chords-in-key card-height fix, a different region of the same file) is covered in [ux-philosophy/expertise-adaptive-display.md](../ux-philosophy/expertise-adaptive-display.md)'s S-DIAGRAM-PREF section, not repeated here.

**U14 - `Sound.playScale()` gains `handle.setTempo(bpm)`** (`shared/sound.js`): queues a new tempo, applied at the SAME next-`step()`-tick boundary `handle.retarget()` already uses (D-EAR-1.5) - the note already in flight keeps its already-scheduled `noteDur`; only the FOLLOWING tick's own scheduling interval changes, so a tempo tap never clicks/gaps the currently-sounding note. A no-op (current tempo continues) for a non-finite/`<=0` bpm, or after `stop()`. `tracks.js`'s new `TEMPO_BPM = {slow:72, med:104, fast:140}` maps a 3-stop `.viewToggle` control (reuses the SAME segmented-control primitive U13's Window\|Full-neck toggle established - zero new CSS for the control itself) sitting directly under the notes/degrees lines it paces. Persisted via `readTempo()`/`writeTempo()` (additive `music.tempo.v1`, default now `'med'` - see [decisions.md](../decisions.md) D-EAR-1.6 for why the default changed, not just the control being added). A tap while playing calls `studioSound.setTempo(TEMPO_BPM[tempo])` (live, no re-tap); a tap while stopped just persists the choice for the next play tap's `opts.bpm`.

**U15 - re-repro found no logic bug; hardened visibility regardless**: live Playwright re-repro across window/full-neck x mode/pentMinor x target-on/off found the D-EAR-1.5 `kx-sounding` mechanism fires correctly in EVERY combo - the operator's report is explained by the visual delta being genuinely too subtle to notice at a glance (light theme especially: `--sound-bg` #efe7fb sits close in luminance to the default `--dg-dot` #dfe6ee fill, same dot radius, no other distinguishing visual weight - a color-only cue two adjacent pastels apart, on a marker that bounces at up to ~2.6 notes/sec at the new Fast tempo). Fix (`tracks.css` EOF `.kx-sounding` rule) is a HARDEN, not a re-derived color: `transform:scale(1.35)` (with `transform-box:fill-box`/`transform-origin:center` so the enlarged circle scales around its OWN center, not the SVG viewport's default 0,0 origin - keeping it centered on its unscaled sibling `<text>`), `stroke-width:2.6 !important` (was the inherited 1.2 inline value), and a `filter:drop-shadow(0 0 2px var(--sound-line))` glow. The existing `--sound-bg`/`--sound-line` fill/stroke VALUES are unchanged (already reviewed/contrast-verified under D-EAR-1) - this is a SIZE + STROKE + GLOW signal, deliberately not relying on hue perception alone.

**U16 - the Legend primitive** (new `shared/legend.js`, `window.Legend`): `Legend.render(classes) -> HTMLElement|null` renders dot-swatch + label rows for a FIXED 6-class vocabulary (`root`, `chord`, `blue`, `ghost`, `rub`, `sounding` - `LEGEND_ORDER`), each swatch a real tiny inline SVG circle using the SAME `kx-*` class + `var(--kx-*/--accent*/--dg-dot*/--sound-*)` CSS-var styling `diagram.js`'s `scale()` applies to the actual fretboard dots (never an approximated/hardcoded hex - a theme/accent change propagates to the legend for free, same as the real fretboard). Replaces the old M-GUIDE W3a target-caption prose sentence ("Showing X inside Y - accent = chord root, filled = chord tones, hollow = chord tone outside the scale.") entirely - the `data-targetcap` container is now `data-legend`. `tracks.js`'s new `legendClassesFor(bundle, scaleId, isSounding)` derives which classes are CURRENTLY VISIBLE from the SAME `computeTones()`/`defaultTones()` the fretboard render itself consumes (never a second, divergent notion of "what's on screen") plus the live `studioSound` truthiness: `'root'` is ALWAYS included (a solo scale always has a root note); `'chord'`/`'ghost'`/`'rub'` only when `computeTones()` actually produced that piece (an inert tap must not show a dead row); `'blue'` only for the Blues scale (`defaultTones()`'s always-on b5); `'sounding'` only while actually playing. `renderLegend()` is called at Studio open, on `toggleTarget()`, on sound play-start/`onStop`, AND on every scale-chip `select()` - the last one is a genuine NEW call site the old caption never needed (its text only depended on `activeTargetChord` + the scale-invariant `keyLabelPlain`, so it "just worked" without re-rendering on a chip switch; the Legend's chord/ghost/rub rows DO vary per bundle, since a target chord's tones can be in-scale for one scale-chip and a ghost for another).

**U20 - the accent-derived note palette**: `tracks.css` (`--kx-chord`/`--kx-blue`) and `songbook.css` (`--sound-bg`/`--sound-line`) each gain an `@supports (color: oklch(from red l c h))` block that RE-DERIVES those colors from `var(--accent)` via CSS Relative Color Syntax instead of a fixed hex pair - `oklch(from var(--accent) L C calc(h + offset))`, only the hue (`h`) rotating: chord `h+30`/blue `h-30` (adjacent, opposite sides of root), sounding `h+180` (complementary - the one class needing MAXIMUM perceptual distance, since it marks a transient state rather than a persistent theory class); root stays `--accent` itself, ghost stays neutral (unchanged `--kx-ghost`). L (lightness) and C (chroma) are PINNED per theme - not derived from the accent's own L/C, which an unusually bright/muted user pick could otherwise swing wide enough to blow the ink-contrast floor - chosen to match the PRE-U20 literal hex figures' weight (full offset/L/C table: [ux-philosophy/component-conventions.md](../ux-philosophy/component-conventions.md) "Note palette"). A literal-hex fallback (the pre-U20 values, unchanged) covers browsers without Relative Color Syntax support. Since `shared/legend.js`'s swatches reuse the SAME `kx-*` classes the real fretboard dots use, a hue-picker change re-colors BOTH surfaces to the identical resolved color automatically - live-verified (getComputedStyle on a real dot vs. its matching legend swatch, before/after an accent change, both themes).

## Global mini-player (PLAYER-FEEL, PR #323) [STABLE]

Playback survives the Studio. The one `elPlayer` overlay gains a SECOND collapse level - `.bt-player.mini` (tracks.css) re-lays the merged header strip (S-STUDIO-HEADERMERGE) as a persistent 56px bottom bar pinned above the tabbar; the iframe is CLIPPED, never removed (the same contract `.bt-st-media.min` proved), so audio continues and NO DOM is rebuilt or reparented across minimize/expand. Lifecycle (tracks.js, mount scope beside `closePlayer`): `minimizeStudio()` (adds `.mini` + `body.miniplayer`, stops synth audition, releases audio warm, wires bar-tap-to-expand), `expandStudio()` (removes `.mini`, re-registers exactly one NavHistory slot), `dismissStudio()` (the registered close fn at Studio open - a yt-backed Studio MINIMIZES on back/dismiss, a videoless one closes fully), and the bar's x (`data-minix`) is the real `closePlayer()` teardown (which also clears mini state). `openStudio(t, {startMini:true})` builds + wires the full Studio then minimizes in the same call - the play-from-row path (autoplay rides the row-tap's user activation). Controller surface (mount return): `nowPlaying()` -> `{key: trackKey, paused} | null`, `togglePlay()` (routes through the strip's real `[data-nppp]` button - one code path for icon, intent, and the `music:nowplaying` CustomEvent dispatched on document at open/minimize/expand/pp/close). Honest state: play-state keys off USER intent (`userPaused`), never YT infoDelivery (which cannot report under blocked egress). Consumers: songbook.js `refreshNowPlaying` class-sweeps rows carrying `data-npkey` (isPlaying/isPaused - never a re-render, the scroll never jumps); list-item.js renders both the ▶ glyph and the 3-bar `.li-eq` equalizer and classes flip which shows; `repertoireAction` is transport when the tapped row IS the playing track. Landscape: `.mini` neutralizes the two-pane split's 44% stage clamp. Scenario: [test/pw/scenarios/mini-player.json](../../../test/pw/README.md) (30 steps incl. mutation-proven red).

**v2 (UAT batch 2, 2026-08-08, music-v323-3):** (1) **One transport owner** ("I see three play buttons"): while the video panel is expanded (`.vidopen` on elPlayer, kept honest by `setMin()`), YouTube's own controls own transport - the bar's pp/progress/time hide and the slot carries a visible **Hide video** CTA (`.bt-st-vidmin`, `data-vidmin`, same collapse path as the menu row; icon-only under 380px). Hiding the video hands transport to the bar. The pp stays in the DOM while hidden so `togglePlay()`'s programmatic click works in every state; `closePlayer()` clears `.vidopen`. (2) **The Jams segment** (songbook.js + play/index.html): All | Jams | Setlist - Jams is a filtered VIEW of All keeping only rows whose `npKeyFor(rec)` is non-null (the same playable predicate that lights now-playing rows, so "in Jams" and "can play" never drift), same search/filter chrome, `practiceOrigin` remembers it for back-restore. Scenario: test/pw/scenarios/uat-batch2.json (mutation-proven red on the Jams filter). (v2's bottom-bar-in-expanded mechanism - absolute against `.bt-studio` - was superseded same-day by v3's fixed one-slot bar.)

**v3 (UAT batch 3, 2026-08-08, music-v323-4) [the standing player model]:** "Make it feel like the same now playing element - use SSOT rendering, don't hide bottom tabs, don't move it - like Spotify/YouTube." **The bar is ONE element in ONE slot**: `.bt-st-head` renders once per open - `[title/meta] [pp|Hide-video] [progress] [time] [x]` - and is `position:fixed` above the tabbar (`bottom: max(safe,8px)+64px`, centered card, max 540px) in BOTH mini and expanded; nothing about it moves or re-renders between states (scenario-proven: rect equality mini vs expanded). **The Studio is a SHEET**: `.bt-player.studio:not(.mini)` gets a raised bottom inset (`+128px` = bar slot + bar + gap), so the bar band AND the tabbar stay visible; back (`.bt-st-back`, F32 standard) + hamburger move to a slim `.bt-st-topbar` at the sheet's top (Spotify collapse-top-left / menu-top-right grammar); the `.bt-st-menu` fly-out anchors under it. **Tabs stay LIVE under the sheet**: a tabbar tap collapses the sheet first via `NavHistory.settleAfter(collapseStudioRaw, switchTab)` (synchronous slot swap, no back/push race - wired in songbook's tabbar handler off the `getStudioExpanded`/`collapseStudioRaw` opts, exported by tracks.js as `studioExpanded`/`collapseStudioRaw`). The bar's x settles the NavHistory slot when tapped in the expanded state (`settleAfter(closePlayer, null)`); mini holds no slot. Mini CSS = "the sheet is gone": zero-footprint overlay, topbar/body/stage chrome hidden, media CLIPPED (never removed). Scenario: test/pw/scenarios/uat-batch3.json (26 steps: don't-move rect proof [mutation-proven red], tabs-visible, tab-tap-collapse, bar-survives-tab-hopping, teardown).

**v4 (UAT batch 4, 2026-08-08, music-v323-5):** the row + bar become fully player-grammar. (1) **Row body tap PLAYS** a playable row (`npKeyFor` non-null -> `repertoireAction`, the same transport/startMini routing as the ▶ glyph); videoless rows keep body = details. `repertoireAction` now guards on the STUDIO TARGET (not `rec.yt` directly), so "can play" is the exact `npKeyFor` predicate everywhere - the setlist path hands raw song records straight in. (2) **The leading details chip** (`.li-lead`, list-item.js primitive): a fixed 44px tap target on every row - the details door (`onLead` -> openRepertoireItem / openPractice); holds the setlist position (`.li-num`, nested - same look, same asserts) when the row has one, an info glyph when it doesn't, so set membership swaps chip CONTENT, never row layout. (3) **Idempotent open**: `openStudio` on the already-now-playing track (same trackKey + same yt) EXPANDS the existing Studio instead of rebuilding - a rebuild would remount the iframe and restart the audio; the curated-url path (new yt) still rebuilds. (4) **The bar pops while live**: `npLive`/`npPaused` classes ride `dispatchNowPlaying` (the same dispatch that lights rows - bar and rows can never disagree); npLive gates an accent outline + glow AND the bar's equalizer - the SAME `.li-eq` primitive rows use (`.bt-st-bareq` composes it), animated playing / frozen paused. (5) **Marquee**: `refreshMarquee()` (called from dispatch, setMin, resize) measures title overflow and arms a shuttle animation on the inner `.bt-st-tx` span (`--mq`/`--mqd`); reduced-motion keeps the static ellipsis. Scenario: test/pw/scenarios/uat-batch4.json (28 steps, body-tap-plays mutation-proven red). Known pre-existing: persona-firsttimer-journey fails on the welcome tour's #wNext stability on origin/main too (verified in a base worktree) - unrelated to the player arc.

**v5 (UAT batch 5, 2026-08-08, music-v323-6):** (1) **Opaque sheet** ("can see rows behind above now playing"): the expanded sheet goes FULL-inset (`inset:0`) so nothing leaks through the band around the bar; the tabbar stays visible + tappable by painting ABOVE it instead - `body.studioopen` (toggled inside `dispatchNowPlaying`, so every sheet transition funnels through one authority) raises `.tabbar` to `z-index:55`, over the sheet (50) and under every real modal (perform 60, invModal/maxOv 70, edit form 75). Scroll clearance for the fixed bar moves onto `.bt-st-body` (portrait) and the landscape `.bt-st-stage`. (2) **Loop marquee** ("slowly left only and wrap around... no switching direction"): `refreshMarquee` appends a second title copy (`.bt-st-tx2`, 48px wrap gap) and the pair slides left by exactly one copy+gap, linear + infinite (~22px/s, 1.2s initial delay) - the wrap point is seamless; reduced-motion keeps the static ellipsis and hides the clone. Asserts updated to hit-tests: the band above the bar resolves to the sheet (never a list row), the tabbar's center resolves to the tabbar (mutation-proven: dropping the z-raise fails it).

**v6 (UAT batch 6, 2026-08-08, music-v323-7) [transport + queue]:** (1) **The queue is the current view**: songbook's `playablePool()` re-derives the playable records from STATE at advance time (All and Jams share one pool - the playable filter IS the Jams filter; the Setlist segment walks the set's hand order; search/genre/key filters apply as rendered); `playNeighbor(dir, shuffle, currentKey)` picks next/prev with wrap (or a random OTHER member under shuffle) and re-opens via `openStudioCb(studioTarget, {startMini:!expanded})` - expanded stays expanded, mini stays the bar. Wired tracks->songbook through `Tracks.mount({advance})` -> `songbookCtl.playNeighbor` (the onEditRequest closure-indirect pattern). (2) **Track-end honesty + auto-advance**: `onTrackEnd()` (one-shot per open) fires from the embed's `onStateChange` ended event (state 0) or a `currentTime>=duration-0.4` infoDelivery fallback (duration>3s); it auto-advances via the same `advance` callback, or with nothing to advance to flips the bar to ► and freezes the equalizers (paused/userPaused + dispatch). Headless/blocked egress never reports - the chain is scenario-proven by SYNTHESIZING the postMessage end event. (3) **prev/next** (`data-npprev`/`data-npnext`, 44px, flanking the pp; visible in vidopen too - YouTube's controls can't playlist-next) + mini bar-tap exclusions extended. (4) **Shuffle** (`data-shuffle`, sheet topbar, the Spotify home): persisted `music.shuffle.v1` (additive key, defensive read - no schema bump), `.on`/aria-pressed state. (5) **Progress track border**: `.bt-st-np-prog` gets `border:1px solid var(--accent-dim)` + 8px box so total extent reads at a glance; sub-380px drops the time label first. Bar gap tightened to 4px to fit [id][prev][pp][next][prog][time][x] at 412. Scenario: test/pw/scenarios/uat-batch6.json (34 steps: transport buttons, bordered track, next/prev round-trip with row lights following, synthesized end -> auto-advance [mutation-proven red], shuffle toggle + persisted + random-jump, teardown). Device-honest caveat: auto-advance rebuilds the embed; if a phone's autoplay policy blocks the new iframe's sound, one ▶ tap resumes - the fix path if UAT hits it is loadVideoById into the persistent iframe.

## Catalog sync pipeline (playlist extraction, PR #322) [STABLE]

The shipped catalog gained a repeatable KEYLESS ingestion path from the operator's curated YouTube playlist - the U18 pipeline ruling (no API key, ever) made executable. `scripts/playlist-sync.js` has two modes: **fetch** (`--playlist <id>`: RSS feed windowed ~15 + playlist-page `ytInitialData` parse for the rest + per-video oEmbed enrich; hints via `YtInfo.parseHints`, storage keys respelled to preferred tonic names via `Circle.preferredTonicName`; diffs vs the catalog by `Tracks.trackKey`; exits 1 on a zero-entry read) and **bake** (`--bake --from <payload> [--skip id] [--dry]`: appends `status:"new"` entries, preserving the file's exact `JSON.stringify(all, null, 2) + '\n'` serialization so diffs are pure appends). Fetch runs on a GitHub Actions runner (`.github/workflows/playlist-sync.yml`, workflow_dispatch - dev-container egress blocks youtube.com); the workflow commits only the payload pair (`docs/plans/playlist-sync-latest.json` + `docs/plans/playlist-veto-latest.md`), NEVER tracks.json - the bake always rides a reviewed PR whose body carries the veto table (merge = red-pen approval). Conventions enforced in code: never mutate an existing row, never fill a yt id onto a track that has one (the merge-localstorage.js convention). Post-bake artist/title enrichment re-keys trackKey, so it ships with `LEGACY_TRACKKEYS` remaps (tracks.js) in the same PR. Schema gate: `test/tracks-catalog.test.js`.

---

**Anchors verified:** tracks.js:196-296 (studioTheory, normMode), ~459-516 (inversionsHref, buildWhy), ~517-687 (openStudio incl. rehydration + scale chips + whynote), targetTones/defaultTones/computeTones/toggleTarget/renderGuide/renderJamPanel/renderFretboard/scaleRenderOpts/readFretView/writeFretView (openStudio region), boxScaleIdFor (S-BLUES-BOXES), solo-guide.js (SoloGuide), shared/jam-queries.js (JamQueries), shared/sound.js `playScale`/`handle.retarget` (M-EAR wave 1.5), diagram.js `scale()` opts.tones + the unconditional `data-pc` attribute (M-EAR wave 1.5) + `scalePlan()`'s `ghostsOn`, key-explorer.js `renderScale`/`setTones`/`setSounding`/`boxes`/`openPcsFromPack`/`POS_CAP` (opts.boxScaleId pager-snap + `.scaleBoxChip`; opts.noPosCtrl + boxWrap.setSounding, M-EAR wave 1.5), tracks.css EOF kx-* / guide / jam / kx-sounding section (incl. `--kx-ghost`, `--kx-chord-ink`/`--kx-blue-ink`, the U2 `.scalePosBtn` 44px override, `.scaleBoxChip`, `.bt-st-jamchips`/`.bt-st-jamquery`/`.bt-st-jamresult`, `.kx-sounding`) + landscape media query, songbook.js ~817-830 (bridge), openEditOrAdd (M3-5A add/curation flow), notables.js, candidates.js, docs/plans/m-guide-ia-20260704.md §§2-3, 5, 8, docs/plans/uat-walkthrough-20260704.md U2-U4, U11-U13; P5 seasoned-player adversarial fold (2026-07-05, PR #118); S-STUDIO-POLISH (2026-07-04); S-BLUES-BOXES (2026-07-04, docs/plans/short-specs-20260704.md); M-EAR wave 1 (2026-07-04, docs/plans/vision-ear-first-20260704.md, shared/sound.js, tracks.js closePlayer/openStudio soundToggle region, tracks.css `.bt-st-solorow`/`.bt-st-degrees`, songbook.css `--sound-bg`/`--sound-line`/`.soundToggle`/`.soundNote`, test/sound.test.js, test/consistency-lint.test.js's `.sound*` semantic-separation assertions); M-TRACKLIB wave 1 (2026-07-04, docs/plans/vision-ear-first-20260704.md, shared/jam-queries.js, tracks.js jamToggle/jamPanel region, tracks.css `.bt-st-jamchips`/`.bt-st-jamquery`/`.bt-st-jamresult`, test/jam-queries.test.js); M-EAR wave 1.5 (2026-07-04, docs/plans/uat-walkthrough-20260704.md U11-U13, shared/sound.js `retarget`, shared/key-explorer.js `setSounding`/`noPosCtrl`/`POS_CAP`, shared/diagram.js `data-pc`, tracks.css `.kx-sounding`, test/sound.test.js §6, test/diagram.dom.test.js, test/key-explorer.dom.test.js); M-EAR wave 1.6 (2026-07-05, docs/plans/uat-walkthrough-20260704.md U14-U16 + U20 (U21 covered separately in expertise-adaptive-display.md), decisions.md D-EAR-1.6, shared/sound.js `setTempo` §7, new shared/legend.js (`Legend.render`/`LEGEND_ORDER`), tracks.js `TEMPO_BPM`/`readTempo`/`writeTempo`/`legendClassesFor`/`renderLegend` (openStudio region, replaces `renderTargetCaption`/`targetCapEl`), tracks.css EOF `.kx-sounding` harden (`transform:scale`/`stroke-width`/`filter:drop-shadow`) + new `.legend`/`.legendRow`/`.legendSwatch`/`.legendDot`/`.legendLbl` section + U20 `@supports` accent-derived `--kx-chord`/`--kx-blue` block, songbook.css U20 `@supports` accent-derived `--sound-bg`/`--sound-line` block, play/index.html legend.js script tag, sw.js CACHE v111 + new CORE path, test/sound.test.js §7 (setTempo cases + OfflineAudioContext fast-vs-slow duration test), test/legend.test.js (new), test/consistency-lint.test.js `.legend` radius-consumer entry + "(d) U20" accent-palette block)
