# Decisions Registry

> **Removals are decisions too.** Deleting a surface/feature gets a registry row like any other ruling - the M-7 incident (a queued mission re-proposing a 3-day-old deliberate removal because it was never registered) is why.

[Wiki](index.md) > Decisions

## Purpose

One registry of load-bearing decisions, their rationale, and owning pages. If a decision has an entry here, it was RULED - propose changes explicitly instead of drifting.

## Core decisions

| ID | Decision | Ruling (one line) | Owning page |
|---|---|---|---|
| FORK-4 | Canonical-sharp spelling app-wide `[TRACKS-#98]` | ONE sharp table; flat input normalizes; flats never render | [theory-engine/note-spelling.md](theory-engine/note-spelling.md) |
| SHARP-TIE | Enharmonic ties render sharp | Deterministic PRODUCT POLICY (professor-classified policy-not-bug), not conservatory practice | note-spelling |
| ROMAN-HYBRID | Mode-local numerals in-key; chromatic labels for borrowed | Same chord relabels bVII <-> VII on mode toggle, by design; harmonic-minor V cased by the chord | [theory-engine/harmonization.md](theory-engine/harmonization.md) |
| D1 | Transpose == song key | One songKey; transposing moves the key; picking a key transposes the progression | [systems/compose-key-system.md](systems/compose-key-system.md) |
| D3 | Mode toggle ALWAYS re-harmonizes | Solo-practice scope: pick key+mode -> harmonized progression -> solo; no freeze-chords nuance | compose-key-system |
| D5 | Chord picker NOT key-locked | In-key \| All adaptive list; blues/borrowed/secondary need the escape hatch | compose-key-system |
| D6 | Compose = flattened one-screen | Fixed top region + one scroller; no accordions (nested-scroll trap) | compose-key-system |
| S2/FORK-3 | Studio wheel is read-only | Teaching aid, not a second key-selection surface - no false affordance | [systems/practice-studio.md](systems/practice-studio.md) |
| GRIP | One-hand instrument-in-hands model | 44px floor, thumb-zone awareness, consequence intolerance | [ux-philosophy/design-principles.md](ux-philosophy/design-principles.md) |
| RAIL | Scroll-rail actions movement-cancelled/mode-gated | The hot right edge fires on scroll-grab | [ux-philosophy/interaction-safety.md](ux-philosophy/interaction-safety.md) |
| JIT | One-shot dismissible notables for guidance | Show once, persist dismissal, single-slot priority arbitration | interaction-safety |
| SOLO-BOUNDARY | 5/6-note SOLO scales never harmonize (amended by D-BLUES-KEY, W2) | Pentatonics are solo-layer only, no triad palette, unconditionally; the blues SOLO scale (6-note, fretboard/degrees) stays solo-layer too - Blues' harmonizing counterpart is the SEPARATE 3-degree BLUES_KEY model, deliberately kept apart from this scale | [theory-engine/solo-scales.md](theory-engine/solo-scales.md) |
| BLUE-NOTE-A | Regime-A blue note spells sharp (`A blues = A C D D# E G`) `[TRACKS-#98]` | FORK-4 one-table consistency beats notation convention until #98; flips to Eb at S-BLUES-B via the one-provider seam | solo-scales |
| D-KEYLESS | Keyless progressions stay keyless through mode change AND transpose | Supersedes codex #90 V1's first-chord-fallback per operator input I4; a mode change while keyless re-qualifies via the shared pure fn but never resurrects a root, and the fly-out stays open; also fixes the F12 dead-mode-chip (re-tapping the current mode with no root re-renders instead of no-op-ing) | [systems/compose-key-system.md](systems/compose-key-system.md) |
| D-FRETS-4STR | 4-string necks default to a 12-fret Studio window, not 7 | `pack.meta.strings <= 4` (uke/mandolin/mandola/cigar box) - a 7-fret window covers less musical value per fret at 4 strings; banjo(5) and guitars keep 7 | [systems/practice-studio.md](systems/practice-studio.md) |
| D-BLUES-KEY | Blues is a separate 3-degree harmonizing key model (I7/IV7/V7), not a diatonic mode | `songbook.js MODES.Blues` (steps [0,5,7], quals all `'7'`) + `Circle.BLUES_KEY`/`bluesKey(root)`; palette-minimalism in-key rule (plain triad or dominant 7th only, no ii/dim/maj7/subs); never auto-inferred; `completions()` top-guarded to `[]`; convertProgressionQualities' Blues<->diatonic directions gated on the professor-fold amendment (dom-7-strip applies only to a root ON the blues palette, offsets {0,5,7} from the SOURCE tonic - not any target-mode degree, so a non-palette root like a user-added A7 survives unchanged) | [theory-engine/harmonization.md](theory-engine/harmonization.md) |
| D-CAP12 | Compose progression cap raised 8 -> 12 (`COMPOSE_MAX`) | Fits the two 12-bar Blues starters; one shared const replaces both `addChord`/`renderProg` gates so it can't drift; operator-visible via the updated `maxNote` copy. The strip originally just scrolled past a handful of chords (`overflow-x`) once the cap grew room to matter - D-PROG-WRAP supersedes that: the strip degrades + wraps instead | [systems/compose-key-system.md](systems/compose-key-system.md) |
| D-PROG-WRAP | (amended U8b) Progression strip renders a COUNT-driven staged density ladder - full diagram cards (1-4), one-row compact tokens (5-6), a fixed 6-per-row compact grid (7-12) - instead of horizontal-scrolling (amends D-CAP12's "strip scrolls" behavior) | ORIGINALLY (S-PROG-WRAP, UAT U8) a pure width threshold: `'compact'` once the measured diagram-card row would overflow the strip, else `'full'` - correct on "no horizontal scroll" but width-only, so an 11-chord progression flex-wrapped as an uneven 5+5+1 (wherever the browser happened to break the line), not a deliberate row shape. S-PROG-WRAP-2 (UAT U8b) keeps `progStripMode`'s measured-width mechanism but makes it a NARROW-VIEWPORT GUARD ONLY (one-stage-early demotion, never a promotion) behind a count-driven candidate stage (`<=4` full, `<=6` fill-row, else grid6, CSS Grid `repeat(6,1fr)` fixed) - so 12 chords is exactly two clean rows of 6 and a short progression keeps generous full-size cards. Reuses `.suggChip`/`.scName`/`.scRn` verbatim in both compact stages (not a 4th chip variant), with the same `.composeWrap.maxed .prog .suggChip` carve-out (now spanning all 3 stages) so the progression's own slots stay tappable at the D-CAP12 cap | [systems/compose-key-system.md](systems/compose-key-system.md) |
| D-TARGET | Chord-tone targeting: in-scale tones filled, out-of-scale tones GHOSTED (amended) | ORIGINALLY intersection-only (a chord tone outside the current scale was a deliberate deferral, no ghost dot). P5 seasoned-player adversarial fold (2026-07-05) KILLED that deferral: for A7 over A blues, C# (the major 3rd) is the money note - hiding it taught the habit the feature exists to break. Now EVERY chord tone renders: in-scale as filled `kx-chord`/`kx-root`, out-of-scale as a hollow `kx-ghost` dot at its correct fret (same per-string fret math, `Diagram.scale`'s `plan.ghostsOn`); `targetTones()`'s `ghostPcs` carries the out-of-scale set. Caption legend gained "hollow = chord tone outside the scale" | [systems/practice-studio.md](systems/practice-studio.md) |
| D-CARDS-STATIC | Per-scale mentor cards (SoloGuide) are curated static prose, not derived | NEW `music/shared/solo-guide.js` (`window.SoloGuide`), loaded before songbook.js AND tracks.js; `framing()` moved verbatim from tracks.js; `card(scaleKey, notes)` interpolates `{i}` degree-index placeholders only - zero theory computed in the module itself (A9) | practice-studio |
| D-LANDSCAPE-FLEX | Landscape two-pane split refined, not restructured | Existing coarse-pointer media-query flex split (grid-areas rejected): stage 48% -> 44%, body padding `12px 12px 18px` -> `10px 12px`, gap `12px` -> `10px` - room for the Guide card + target caption in the same short-height pane | practice-studio |
| D-SAVE-TRUTH | USER-initiated saves get truthful feedback; passive persistence fails soft | safeSet() is the single write seam for saveCustom/saveSet/saveLast/savePerfPrefs/saveSongView (A1 fix, analysis-refactor-enhance-20260704); saveProgression branches its toast on the real write result instead of claiming success unconditionally; passive writes (last-opened, perform prefs, song view) console.warn once per key and stay silent in the UI - no per-keystroke nagging | [systems/data-model.md](systems/data-model.md) |
| D-HARDEN | Four mechanical hardening seams, one PR, all behavior-preserving except one bug fix (analysis-refactor-enhance-20260704 A4/A5/A6) | (1) `ListItem.wireTap` is now the SSOT movement-cancel tap guard - `Songbook.wireTapCancel`/`composeWireTap` delegate to it (3 copies -> 1); (2) `music/shared/esc.js` is now the SSOT HTML-escaper (strict `&<>"'` superset of every prior variant) - list-item/tracks/notables(removed, was dead)/repertoire-form/diagram/songbook all delegate (~8 copies -> 1); (3) `test/sw-verify.test.js` asserts every CORE path exists on disk AND every shared/*.js `<script src>` in play/index.html is CORE-precached - caught + fixed 3 pre-existing gaps (list-item.js/repertoire.js/repertoire-form.js were script-tagged but never cached), `scripts/check-cache-bump.sh` separately guards the CACHE-vs-diff invariant (needs git history, not unit-testable); (4) `toggleSet`'s "Added to setlist" toast now branches on `saveSet()`'s real result (same D-SAVE-TRUTH bug shape, PR #116 follow-up) - the ONLY behavior change in this mission | [systems/runtime-architecture.md](systems/runtime-architecture.md), [systems/data-model.md](systems/data-model.md) |
| D-CHIPS-PLUS | Blues-key solo preview row gains a Mixolydian chip; every chip gains a degrees line (S-CHIPS-PLUS, P5 W3 enhancement asks) | The freed 4th Blues-key chip slot (opened by the mode-chip dedup) is Mixolydian - the dominant-scale option a player actually reaches for over I7/IV7/V7 - not a repeated Blues option; resolved independent of `keyMode` via `Circle.spellScale(root, 'mixolydian')`, same pattern as the existing pentMajor/pentMinor/blues chips. New `soloChipDegrees(keyMode, scaleId)` mirrors `soloChipScale`'s own scaleId routing exactly so a chip's notes and its degrees line always describe the SAME scale; caption falls back to `SoloGuide.card('mixolydian').chooseWhen` since `framing()` has no mixolydian branch. P5's separate bar-by-bar target-emphasis ask (auto-advancing per-bar 3rd/b7/root targets with playback) stays UNBUILT - D-TARGET's ghost-dot fix already answers "which note now" for a tapped chord; the playback-driven auto-advance remainder is explicitly deferred (LONG candidate, no spec yet) | [systems/compose-key-system.md](systems/compose-key-system.md) |
| D-BACKUP-NUDGE | One-shot backup-staleness nudge via the free LOWEST Notables priority slot (analysis-refactor-enhance-20260704 B2) | `notables.js` PRIORITY gains `'backup'` after `firstrun`/`whynote`/`roman` - never preempts onboarding/theory guidance; pure eligibility in `backup.js` (`songCount` + `backupNudgeState`: eligible at `>= NUDGE_MIN_SONGS` (3) songs AND never-backed-up-or-`>= NUDGE_STALE_DAYS` (30)-days-stale); all DOM wiring lives in `play/index.html`'s Settings script (`renderBackupNudge`, `#backupNudgeSlot`) - zero songbook.js changes; tap opens Settings (no new UI surface); auto-RELEASES (not dismiss) the instant `markBackedUp()` runs so the banner can't linger showing stale advice | [systems/data-model.md](systems/data-model.md) |
| D-BLUES-BOXES | Named Box 1-5 positions ship as pure pitch-class math, not a curated per-instrument fret table | `KeyExplorer.boxes(rootPc, scaleId, openPcs)` walks the pentatonic-minor scale's 5 degrees up the lowest string (index 0), wrap-folding any start past the pager's fret-14 cap back down an octave (the classic "Box 5 sits just below Box 1" shape falls out of this for free); blues rides the SAME 5 windows as pentMinor (the b5 never earns its own box); pentMajor's windows derive from its relative-minor root (rootPc-3) but each box's labeled root-string anchor re-solves for the ACTUAL major rootPc. moveHint is template-generated (A9) off the fret delta to the next box in NECK order (ascending startFret), not box-number order. Generalizes to every instrument profile via `openPcsFromPack` (profile string-name order, not acoustic pitch - so ukulele's re-entrant G string is still treated as "the lowest string" per this codebase's existing index-0 convention) | [theory-engine/solo-scales.md](theory-engine/solo-scales.md), [systems/practice-studio.md](systems/practice-studio.md) |
| D-SHAPE-CLASSIFY | S-DIAGRAM-PREF step 0 (shape classifier) ships as curated per-profile templates matched by NORMALIZED fret pattern, not a name-based lookup - and root/inversion derive from REAL pitch-class math, deliberately the opposite simplification from D-BLUES-BOXES | `music/shared/shape-classify.js` (`window.ShapeClassify`), scope `guitar-standard` + `ukulele-gcea` only (every other profile null/`[]`). `classify(profileId, chordName, frets)` normalizes the input (subtract its own lowest fretted value) and matches against a curated per-quality template table - movable-template-aware, so a barred voicing classifies under its OPEN-position ancestor's family (e.g. guitar F = `barre-E`, ukulele E major = `barre-D`) rather than a new family per root. UNLIKE D-BLUES-BOXES' `openPcsFromPack` (which deliberately treats string index 0 as "the lowest string" for box-position pedagogy), this classifier's `rootString`/`inversion` derive from the ACTUAL lowest-SOUNDING pitch (frequency-based semitone math, not index order) - the only way to get a correct inversion label on ukulele's re-entrant GCEA tuning, where the C string (index 1) genuinely sounds lower than the "4th" G string (index 0). A cross-check against every quality bucket in both profiles caught one real duplicate: play/index.html's synthetic "Em-shape closed barre" template is, once normalized, byte-identical to the open Dm chord's own shape - so that template was NOT re-added here; a derived voicing built from it correctly (and more honestly) classifies as `barre-D`. Dim7 voicings and a handful of genuinely distinct open-position cowboy fingerings (guitar B7/Fmaj7/Bm7) are honest nulls, never guessed | [ux-philosophy/expertise-adaptive-display.md](ux-philosophy/expertise-adaptive-display.md), [../../test/shape-classify.test.js](../../test/shape-classify.test.js) |
| D-EXTRACT | buildAdapter + the SUGG chord-follower table move out of play/index.html's inline bootstrap VERBATIM into tested shared modules, no logic changes (analysis-refactor-enhance-20260704 A3/A7) | `music/shared/chord-pack-adapter.js` (`window.ChordPackAdapter.buildAdapter`) and `music/shared/sugg.js` (`window.Sugg.SUGG`), both UMD-tailed (module.exports + window global, same pattern as esc.js/circle.js) so they're require()-able in Node. `test/live-adapter.test.js` REWRITTEN from a regex-extraction + `new Function()` sandbox (covered only the enharmonic profileVoicing path) to direct-require against REAL guitar-standard/ukulele-gcea profile fixtures - now covers the movable-template math, the ukulele artifact-triad augmentation, the I-IV-V chain's Position-1/Position-2 fallback, and guitar's chainVoicingsBackoff path (26 assertions, up from 5). New `test/sugg.test.js` asserts every SUGG key + follower chord name is already FORK-4 canonical-sharp (a local flat->sharp table duplicated in the test, not imported from circle.js) plus a content-identity snapshot against the pre-move literal. ONE necessary substitution during the move: `adapter.init()`'s `el('quickTune')` call is inlined to `document.getElementById('quickTune')` since `el` was play/index.html's own page-local helper and can't cross the module boundary - functionally identical | [systems/runtime-architecture.md](systems/runtime-architecture.md), [systems/instrument-profiles.md](systems/instrument-profiles.md) |
| S-NAVHIST | Compose modal transitions that open a new layer use `NavHistory.settleAfter()` directly, never `NavHistory.dismiss()` (PR #144 finding) | `openSoloChoiceRow`'s Save/Skip and `openSaveNameRow`'s Save/Enter route through `settleAfter(rawClose, runNext)` from their own button/backdrop/Escape handlers instead of `dismiss()`+`history.back()`+popstate - dismiss() closes via nav-history.js's popstate `while` loop, and a nested `NavHistory.open()` call made from a closeFn running INSIDE that loop re-triggers its `stack.length` recheck, double-popping the just-opened layer immediately shut (the Studio, or the save-name row one step earlier, used to flash open then close on a never-saved progression's "Solo over it -> Skip"/"Save & open Studio"). Cancel/backdrop/Escape on the name row never open a layer, so they stay on plain `dismiss()`. The one un-interceptable case - a genuine hardware/gesture Back, which fires a real popstate directly - still resolves via the registered closeFn, but its `onPick`/`done` delivery is deferred one tick (`setTimeout(fn, 0)`) so the resulting `NavHistory.open()` runs clear of the popstate loop | [systems/runtime-architecture.md](systems/runtime-architecture.md) |
| D-DIAGRAM-PREF | S-DIAGRAM-PREF steps 1-2 (Notables prompt + 'patterns' render) ship with `diagram.js` staying 100% agnostic of the pref and of ShapeClassify - both live in a NEW dedicated module instead | `music/shared/diagram-pref.js` (`window.DiagramPref`) owns `music.diagram.pref.v1` (get/set, defensive read, default 'dots') and `labelFor(profileId, chordName, frets)` (returns '' unless pref is 'patterns' AND the voicing classifies). `diagram.js`'s `render()` only gained an optional `opts.patternLabel` string (EXTEND-not-overlay, mirrors `opts.tones`) and a `notifyRendered()` hook that dispatches `music:diagram-rendered` on every real chord render - the trigger `play/index.html` listens for to mount the one-time prompt at "the first chord-diagram surface" without wiring anything into songbook.js/tracks.js/key-explorer.js. Post-rebase past D-EXTRACT (above), the patternLabel wiring lives in `music/shared/chord-pack-adapter.js`'s `diagram`/`diagramClosed`/`diagramChain`, not inline in play/index.html. `notables.js` PRIORITY gains `'diagrampref'` between `'roman'` and `'backup'`; eligibility also explicitly requires `firstrun` to be dismissed already (priority arbitration alone doesn't guarantee "never before first-run"). Picking an option or dismissing via [x] are equally final (Notables' real one-shot semantics - no invented "re-offer" behavior). A Settings row (Dots/Patterns segmented control, mirrors the Theme control) ships as the always-available setter, not deferred. **Deviation ruled explicitly:** the spec's "strip finger-number instruction chrome" / "keep... root/degree tinting" describe chrome that has never existed on the chord-diagram renderer (only the separate solo-scale view has tinting, gated by its own `opts.tones`) - both are no-ops on the real codebase; the only concrete render change is the additive classifier-label caption. Curated per-string fingering-as-text has no data source anywhere in the app - named follow-up, not built | [ux-philosophy/expertise-adaptive-display.md](ux-philosophy/expertise-adaptive-display.md), [../../test/diagram-pref.test.js](../../test/diagram-pref.test.js) |
| D-YT-PREFILL | M-TRACKLIB wave 2a (U17) ships YT-prefill using YouTube's OWN oEmbed endpoint as the primary (not fallback-first) network strategy - a `noembed.com` fallback is implemented and unit-tested but, per a live Playwright CORS check, is never actually needed | **Live verdict (2026-07-05, real Chromium, non-YouTube origin):** `fetch('https://www.youtube.com/oembed?url=...&format=json')` succeeds directly from a plain `http://localhost` origin - `res.ok` true, JSON parsed, real title/author returned, zero console errors. YouTube's oEmbed sets a CORS policy permissive enough that a keyless cross-origin GET just works; the `noembed.com` fallback (kept as defense-in-depth, unit-tested in [yt-info.test.js](../../test/yt-info.test.js)) never fired in this check. Not separately re-verified against the production `nhruska.github.io` origin - CORS success here is a response-header property (not caller-origin-specific), so this is expected to generalize, but flag if a production paste ever behaves differently. `shared/yt-info.js` (`window.YtInfo`) owns `fetchInfo`/`parseHints` (pure, exported, 49 unit tests incl. a realistic 20-title fixture matrix); `repertoire-form.js` gains `applicableYtHints` (pure apply-to-empty-only decision fn, unit-tested) + `wireYtSuggest` (DOM glue: paste/blur listeners, pending/ready/hidden suggest row) - live Playwright-verified: real oEmbed round-trip fills empty title+artist and hides the row; a pre-typed title survives untouched while the empty artist still fills; invalid pasted text makes zero network calls; a true offline network makes the row stay hidden with zero uncaught JS errors (Save still succeeds). Zero new persisted state (see [data-model.md](systems/data-model.md) YT-prefill section) - the suggestion only writes into the form's 5 EXISTING fields (title/artist/key/mode/genre); `bpm` has no field yet and surfaces as a caption-only note. Named scope note: the Mode `<select>` has no true "empty" value (unlike Key), so its untouched-sentinel is the rendered default `'major'` - a deliberate approximation, not a form restructure | [systems/data-model.md](systems/data-model.md), [../../test/yt-info.test.js](../../test/yt-info.test.js), [../../test/repertoire-form.test.js](../../test/repertoire-form.test.js) |
| D-REL-NAMES | S-REL-NAMES (UAT U23) amends D-CARDS-STATIC: guidance copy that references a derivable musical object NAMES the concrete instance for the current key, not just the relationship - KEEP-BOTH (adds a naming capability, changes no existing claim) | Operator (U23): "the guidance says this scale works because F major and its relative minor are the same notes - but doesn't say WHAT the minor key is... a message on the continuum of your journey, available at point of need." `shared/solo-guide.js` gains a pure `relNames(root, scaleId) -> { relMinor, relMajor }` resolver - a thin pass-through onto `Circle.relativeMinor`/`relativeMajor` (Circle already owns this math; nothing re-derived, same A9 zero-theory-in-this-module discipline D-CARDS-STATIC established) - plus `{relMinor}`/`{relMajor}` template tokens (a SEPARATE interpolation pass from the existing `{i}` degree-index tokens, disjoint regexes so both coexist). `framing(scaleId, family, root)` and `card(scaleKey, notes, root)` each gain an OPTIONAL 3rd `root` arg; every existing 2-arg call keeps producing byte-identical output (root absent -> the pre-S-REL-NAMES relationship-only wording, verified in solo-guide.test.js). Two template spots actually reference a derivable-but-unnamed object today - `pentMajor`'s `framing()` line and its `shapes` card block (both "...relative minor pent...") - both now carry `{relMinor}`; no other block in the 7-key x 5-block table references one. `pentMinor`/`aeolian` have no equivalent "relative major" prose in the current adjudicated copy to update, so none was invented - `relMajor` is computed and exposed for the FIRST future line that needs it, without requiring a second resolver later. Normalizes one word: `framing()`'s no-root fallback changes "its relative minor pent" -> "the relative minor pent" (now identical to the shapes card's own always-used fallback) - wording only, the relationship/claim is unchanged. Callers: `tracks.js`'s `renderGuide()`/`select()` (Practice Studio) pass `th.key` (the Studio's own canonical root, same for every scale-chip); `songbook.js`'s `soloChipCaption()` gains an optional 2nd `root` arg, `selectChip()` passes `keyRoot` (the Compose key-view's own key). `music/sw.js` CACHE bumped v113->v114 (no new/removed CORE paths) | [ux-philosophy/design-principles.md](ux-philosophy/design-principles.md), [ux-philosophy/component-conventions.md](ux-philosophy/component-conventions.md), [systems/practice-studio.md](systems/practice-studio.md), [../../test/solo-guide.test.js](../../test/solo-guide.test.js), [../../test/songbook.test.js](../../test/songbook.test.js) |

## Sprint-1 amendments (adversarial-review v2, all folded + shipped in v78)

| ID | Amendment (one line) |
|---|---|
| A1 | Stacked-PR protocol: stacked items target the base branch; retarget+rebase after base merges; never delete a stacked base early; record tested base SHA |
| A2 | File-ownership map before parallel spawns; overlapping regions re-sequence |
| A3 | Clear-undo contract: full-state snapshot; ANY mutation invalidates; route-local session-only banner |
| A4 | Notables: one versioned key music.notables.v1; claim/dismiss API; priority firstrun > whynote > roman |
| A5 | Double-fire prevented by single-slot arbitration + simultaneous-claim tests |
| A6 | Undo persistence explicit: session-only (resolved by A3) |
| A7 | Fold gate: Compose fixed-top bottom edge +-2px at 412x915, verified before/after combined merges |
| A8 | Wave-2 #98 seam named: spellKeyAware/spellScaleKeyAware/keyLabel ONLY; absent -> BLOCKED-not-improvised `[TRACKS-#98]` |
| A9 | JIT copy = static templates interpolating already-rendered labels; zero new theory derivation |
| A10 | SW CACHE bump in the same commit as any CORE change; parallel conflicts resolve max+1 |
| A11 | localStorage test isolation via the shared reset helper |
| A12 | Stacked PRs re-run CI after final base update |

Source: [docs/plans/ux-sprint-1-20260703.md](../../docs/plans/ux-sprint-1-20260703.md) v2 resolution.

## Mission decisions (operator-ruled)

| ID | Ruling |
|---|---|
| M3-CHOICE-A | Zero M3 slices in sprint 1 (council-unanimous, operator-approved); M3 = sprint-2 primary, fresh session, post-#98, 5A/5B pre-answered |
| WAVE-2-DEFER | S-TONES / S-ROMAN+S-KEYPOLICY / S-GOLDEN-B deferred-with-reason on #98; resume on merge event `[TRACKS-#98]` |
| S-BLUES | Pentatonic major/minor + blues BUILT this mission (I1), solo layer only, per the [IA seam contract](../../docs/plans/wiki-ia-20260704.md) |
| S-BLUES-B | Key-aware pentatonic/blues spelling QUEUED on #98 (provider swap only) `[TRACKS-#98]` |
| S-DIAGRAM-PREF | Step 0 (shape classifier, see D-SHAPE-CLASSIFY) SHIPPED 2026-07-05; steps 1-2 (Notables prompt + 'patterns' render, see D-DIAGRAM-PREF) SHIPPED 2026-07-06. Spec home [ux-philosophy/expertise-adaptive-display.md](ux-philosophy/expertise-adaptive-display.md) |
| DECOM | Legacy docs absorbed into this wiki then stubbed/deleted per the IA disposition table (grep-before-DELETE) |
| P5-FOLD | P5 seasoned-player adversarial pass on PR #118 (2026-07-05), folded pre-merge into the same PR | Two changes: (1) ghost dots for out-of-scale chord tones - see D-TARGET amendment; (2) rewrote most SoloGuide card blocks toward chord-relative ("target the CURRENT chord") advice + fixed the pentMajor relative-minor-pent distance (three frets, not two) - supersedes section-8B wherever both touched the same block (dorian.hangOn, pentMinor.startEnd, blues.resolveTo) |
| M3-5A | Finder rehome on songs/tracks merge: DISSOLVE (5A) - curation to +Add/per-item edit, circle panel into the Studio; option 5B (playable-filter mode) rejected | operator 2026-07-04 | [uat + queue](../../docs/plans/QUEUE.md) |
| D-TEMPO-REMOVED | tempo.js + test deleted as dead code (zero consumers since inception; operator: never used it). Git history preserves; re-roll a beat clock only if the tutor roadmap earns it | operator 2026-07-04 | analysis B1 disposition |
| D-CAP12-CONFIRMED | 12-chord Compose cap confirmed by operator after live testing (veto window closed) | operator Tier-1 pick 2026-07-04 (1.4) | - |
| D-STORAGE-LS-MIGRATE | Client storage stays localStorage + versioned schema inventory + migration runner (gh #77 CLOSED by this decision; #76 becomes the spec) | operator Tier-1 pick 2026-07-04 (1.2); IndexedDB explicitly rejected at current scale | [data-model](systems/data-model.md) |
| D-SELECTED-ACCENT | ONE selected-state grammar app-wide: accent-fill (chips' language); .modeSwitch adopts it | operator Tier-1 pick 2026-07-04 (atomic-queue-plan 1.1); build item S-SELECTED-GRAMMAR SHIPPED 2026-07-04 (M-DESIGN-ENFORCE wave 1, D-ENFORCE-1): `.modeSwitch button.on` + `.viewToggle button.on` now `background:var(--accent);color:var(--on-accent)`, matching `.chip.on` | [atomic plan](../../docs/plans/atomic-queue-plan-20260704.md) |
| D-STORAGE-MIGRATE-RUNNER | New `music/shared/storage-migrate.js` (`window.StorageMigrate`) generalizes the ad-hoc migration pattern (songbook.js's perfprefs v1->v2 fallback, tracks.js's migrateUrls/LEGACY_TRACKKEYS) into one `register(fromVersion, fn)`/`run(storageLike)` seam; composes with (does not replace) backup.js's own SCHEMA_VERSION/MIGRATIONS engine, which stays scoped to OWNED_PREFIXES data-map transforms for backup/restore | M-6 STORAGE-MIGRATE implementation of D-STORAGE-LS-MIGRATE (gh #76); ships CURRENT=1, zero migrations - behavior-preserving no-op baseline; wired via ONE script tag in play/index.html (self-runs at load, before backup.js/songbook.js); 3 composition gaps named as follow-ups (backup.js EXCLUDE + restore integration, tri.* backup coverage) - out of this mission's read-only-backup.js grant | [data-model](systems/data-model.md) |
| D-BACKUP-INTEGRATE | Closes all 3 D-STORAGE-MIGRATE-RUNNER follow-ups, keeping both halves (version-in-envelope AND restore-replays-the-runner) rather than picking one | (1) `restore()` now calls `StorageMigrate.run(store)` immediately after a successful atomic write, guarded (`typeof StorageMigrate !== 'undefined'`) since only play/index.html wires the runner - triad-inversions.html never loads backup.js, so restore() is unreachable there regardless; (2) `music.schema.version` STAYS OUT of backup.js's `EXCLUDE` array - deliberately reversing storage-migrate.js's own header note, which had suggested excluding it. The version marker must travel WITH the data: excluding it would leave the DESTINATION device's own pre-existing marker (or none) sitting over freshly-restored, possibly-older-shape data, and `StorageMigrate.run()` would read "already current" and silently skip a migration the restored data still needs. Keep-both is the correct pair, not two independent fixes; (3) `tri.*` (triad-inversions.html) joins `OWNED_PREFIXES` - the pre-existing gap the M-6 inventory surfaced is now backed up/restored | [data-model](systems/data-model.md) |
| D-TOAST-PRIMITIVE | ONE shared `toast.js` primitive owns per-host auto-hide TIMERS only; the Library toast (fixed-overlay fade) and Compose toast (inline `[hidden]` row) KEEP their own distinct visual mechanics rather than being forced onto one shared look | UAT U9 (2026-07-05, operator screenshot): both toasts used to share a single `var toastTimer` inside `Songbook.mount()`'s closure (declared twice, hoisted to one variable) - a `showComposeToast(..., persist: true)` call could silently cancel the Library toast's pending auto-hide, leaving "Added to setlist" stuck on-screen forever. Fix is timer-isolation (a per-host `Map`), not a visual unification - the two toasts serve different placements (fixed overlay vs inline row) and forcing one shape would be a UX change nobody asked for, not a bug fix | [ui-primitives.md](ux-philosophy/ui-primitives.md) |
| D-LAYOUT-SSOT | Chord-tile/diagram layout constants get ONE declared token block (`--dg-canvas-w`/`--tile-min`/`--tile-gap`/`--prog-tile-min`), not a forced merge of every layout rule | `--dg-canvas-w:86px` promotes the previously prose-only "6-string guitar SVG is 86px" figure into a real declared value; `--tile-min` derives from it via `calc(var(--dg-canvas-w) - 2px)` so the two numbers can't silently drift apart again. `diagram.js`'s `SIZES.small`/`SIZES.big` stay UN-tokenized (cross-referenced only) - they're computed per string-count at render time, not a fixed pixel value a CSS var could hold; forcing that coupling would be a false SSOT. `.prog .slot .chord`'s `--prog-tile-min` is independent-by-design (a touch-floor for a horizontally-scrolling strip, not a squeeze-fit value like `--tile-min`). New `scripts/layout-check.py` (shared Python-Playwright venv) render-verifies 360/412/768/1440px x 1.0/1.3 font-scale against `guitar-standard` (widest canvas) for tile overlap + content spill + scale-fretboard containment - the automated guard for the U5/#96 bug class. KNOWN GAP surfaced, not fixed here: `tracks.css`'s `.bt-st-chordcell` (Practice Studio chords-in-key row) has no equivalent SVG max-width clamp - same bug class, different location, out of this pass's required scope | [systems/layout-tokens.md](systems/layout-tokens.md) |
| D-ENFORCE-1 | M-DESIGN-ENFORCE wave 1 ships the first enforcement pass of THE ELEMENT CONSISTENCY LAW: (E1) a dedicated GUIDANCE surface token pair, distinct from SELECTION surfaces; (E2) ONE selected-state grammar app-wide (closes D-SELECTED-ACCENT); (E3) 5 radius-by-role tokens; (E4) a static lint that makes E1-E3 self-enforcing | (E1) `songbook.css` gains `--guide-bg`/`--guide-line`, computed off `--txt-dim` (NOT `--brass` - an earlier draft tried brass, but brass is itself an accent-mix and converged back toward the accent-deep family once diluted, confirmed too subtle in a live Playwright screenshot; `--txt-dim` is genuinely accent-independent at any user-chosen hue). `.notableBanner` (the U10 finding: it shared `.setUndo`'s accent-deep/accent-dim SELECTION surface) now consumes these tokens plus a `--brass` left accent stripe; its dismiss icon switched from `--accent-ink` to `--txt-soft` (measured below the 4.5:1 contrast floor against the new bg in light theme at the default accent hue). Screenshot-verified side-by-side against `.listItem.inSet` at 412x915, both themes. (E2) `.modeSwitch button.on` / `.viewToggle button.on` converge from `background:var(--surface-2);box-shadow:0 0 0 1px var(--line-strong)` onto `.chip.on`'s `background:var(--accent);color:var(--on-accent)` - no border-color override needed (base rule has `border:none`). (E3) Five tokens (`--r-btn:11px`, `--r-btn-sm:10px`, `--r-chip-pill:18px`, `--r-card:13px`, `--r-input:10px`) - VALUES are the actual per-class inventory majority, not a fixed default (`--r-card` is 13 because 6 of 8 card-shaped consumers already agreed on 13; `--r-chip-pill` is 18 by USAGE weight since `.chip` is the app's one reused-everywhere chip primitive, not raw rule-count). ~30 consumers tokenized across songbook.css/tracks.css/repertoire-form.css; ~18 genuine oddballs migrated to their class's token (full before/after table in the shipping PR body). Deliberately NOT touched: chord-diagram tiles (S-LAYOUT-SSOT's own domain), modal/overlay boxes (their own documented 14-16px range), and `.search`/`.bt-in` (component-conventions.md's documented 9-12px accepted input range, not a scatter finding). (E4) New `test/consistency-lint.test.js` statically guards all three: no SELECTION token in `.notableBanner` (and vice versa); no `.xxx.on{...}` rule anywhere combines a surface-2 background with a box-shadow ring; every tokenized consumer's `border-radius` is a `var(--r-*)` reference, never a re-hardcoded literal | [ux-philosophy/component-conventions.md](ux-philosophy/component-conventions.md), [ux-philosophy/ui-primitives.md](ux-philosophy/ui-primitives.md), [systems/ssot-registry.md](systems/ssot-registry.md), [../../test/consistency-lint.test.js](../../test/consistency-lint.test.js) |
| D-HERO-REMOVED (retroactive) | Library Jam-now/quick-pick hero REMOVED (commit 8cf0647, 2026-07-01, operator interview): its show/hide-on-filter behavior was distracting and the PR62 Genre/Key filter bar superseded its get-me-to-a-song-fast purpose. The jam:true field is RESERVED, not wired - any future jam entry point is a NEW UX decision, not a restoration | registered 2026-07-04 after the M-7 stop | [pr62-uat-followup-atomic.md](../../docs/plans/pr62-uat-followup-atomic.md) |
| D-EAR-1 | M-EAR wave 1 ships scale-audition on BOTH the Practice Studio AND the Compose key preview - KEEP-BOTH (interview Q4), not one or the other | operator interview answer 2026-07-04 ("we need to hear along with the visuals for cognitive capture" - both surfaces, not a single canonical location): zero-dependency Web Audio (`shared/sound.js`, no Tone.js) with a documented `voice()` swap seam for a future Karplus-Strong/sampled provider (#88); slow hum-tempo (~72bpm) octave-folded loop + a bouncing `.sounding` marker across per-note notes/degrees token spans; a THIRD `--sound-bg`/`--sound-line` token pair, genuinely distinct from SELECTION (`--accent-deep`/`--accent-dim`) and GUIDANCE (`--guide-bg`/`--guide-line`) per the Element Consistency Law (D-ENFORCE-1) - guarded by new `test/consistency-lint.test.js` semantic-separation assertions. Only one playback app-wide (starting one silences the other); the Compose preview's decoupled/non-persisted invariant (W3b) stays binding - the toggle/marker never read or write songKey/progression | [systems/practice-studio.md](systems/practice-studio.md), [systems/compose-key-system.md](systems/compose-key-system.md), [systems/ssot-registry.md](systems/ssot-registry.md) |
| D-TRACKLIB-1 | M-TRACKLIB wave 1 jam-discovery explore panel lives in the Practice Studio only - KEEP-BOTH is N/A here (unlike D-EAR-1): NOT the Library, NOT a D-HERO-REMOVED-style show/hide-on-filter surface | operator directive 2026-07-04 ("the appendix is the golden nugget" - docs/plans/vision-ear-first-20260704.md M-TRACKLIB): new `shared/jam-queries.js` (`window.JamQueries`, pure `genresFor`/`feels`/`jamQuery` - curated per-scale genre lists keyed to the SAME 7-scale vocabulary solo-guide.js's Guide card uses, shuffle-vs-straight absorbed into genre labels rather than a 4th axis) drives a "Find a jam" disclosure panel (composes `.bt-st-why-toggle`/`.bt-st-why` verbatim, genre/feel chips compose the shared `.chip` primitive - no new chip variant) under the Solo-over-it section; the query is KEY-AWARE at tap time (current `th.key` + whichever solo-scale chip is active, re-derived live on every chip switch - scale-context reactivity, explicitly NOT the forbidden filter-driven show/hide D-HERO-REMOVED retired). "Add to library" reuses the EXISTING `opts.onEditRequest` -> songbook.js `openEditOrAdd` prefilled-create seam (key + mode forward correctly today); genre does not yet forward through that seam (`openEditOrAdd` doesn't copy `t.genre` onto the form item, and `repertoire-form.js` has no bpm field at all) - named follow-up, not a blocker, since the genre/feel selection is already visible in the panel's own query caption right next to the Add button | [systems/practice-studio.md](systems/practice-studio.md), [systems/ssot-registry.md](systems/ssot-registry.md), [../../test/jam-queries.test.js](../../test/jam-queries.test.js) |
| D-EAR-1.5 | M-EAR wave 1.5 amends D-EAR-1's "a scale-chip switch stops playback" behavior to RETARGET instead - a chip switch mid-audition keeps the loop playing (live A/B compare), never re-tapping; the fretboard also joins the notes/degrees `.sounding` marker (all octaves of the sounding pc light at once); an optional Window\|Full-neck view (0-14 span) supplements the existing windowed pager - KEEP-BOTH with D-EAR-1 (this amends, does not replace, the wave-1 audition decision) | operator audition-round UAT minutes after v106 shipped (docs/plans/uat-walkthrough-20260704.md U11-U13, verbatim: "need to switch modes and keep playing without hitting play again" / "light up the notes on the fretboard" / "show more fretboard instead of pieces, optionally"): `Sound.playScale()` gains `handle.retarget(newPcs)` - queues the swap, applies at the NEXT tick (no click/gap), restarts the ascent at index 0 of the new scale; `diagram.js`'s `scale()` now stamps `data-pc` on every dot unconditionally (deliberate, reviewed change to the tones-absent SHA-256 lock) so `key-explorer.js`'s new `boxWrap.setSounding(pc)` can class-swap the correct dots without a re-render (mirrors `setTones()`'s position-preserving precedent); `.kx-sounding` (tracks.css) reuses the SAME `--sound-bg`/`--sound-line` pair D-EAR-1 already established (Element Consistency Law - one primitive, two surfaces, not a fourth hue) with the one sanctioned `!important` (it's JS-classList-applied post-render, unlike every other inline-styled kx-* class). Full-neck mode requests `frets: KeyExplorer.POS_CAP` (14) + `opts.noPosCtrl` (hides the pager/box-chip without dropping the pack's 6-arg render contract) - "small necks (12-fret default) treat full-neck as 0-14 too," so this ignores the per-instrument `defaultFrets()` default entirely in full mode; persisted via an additive `music.fretview.v1` localStorage key (default 'window', defensive read/write) | [systems/practice-studio.md](systems/practice-studio.md), [systems/ssot-registry.md](systems/ssot-registry.md), [../../test/sound.test.js](../../test/sound.test.js), [../../test/diagram.dom.test.js](../../test/diagram.dom.test.js), [../../test/key-explorer.dom.test.js](../../test/key-explorer.dom.test.js) |
| D-DIM-SHAPES | S-DIM-SHAPES closes the step-0 dim/dim7/aug gap in `shape-classify.js` - KEEP-BOTH: curate real (dim) AND defensive (dim7/aug) buckets, not just the one with a live UI anchor | UAT U21 (operator, 2026-07-05, screenshot): a chords-in-key Bdim card rendered diagram-only, no shape label, because step 0 explicitly left dim/dim7/aug uncurated (honest null). `dim` gets a real curated bucket for both profiles (`dim7-shape` family, single symmetric movable voicing per profile - guitar `[-1,1,2,0,2,0]`, ukulele `[0,1,0,1]` - since both profiles' own "Xdim" chord tables are actually 4-note fully-diminished-7th voicings, not 3-note triads); `dim7` is curated defensively from the SAME physical voicing (no profile chord is literally named "Xdim7" today, but a future Compose free-text entry might spell it that way for the identical sound); `aug` is curated defensively too (`Circle.diatonic()` never emits an augmented triad for any of the app's 7 supported modes, so it has no live chords-in-key anchor either) as the profile's own open-C-major shape with the 5th sharped one fret (family `C`, reused - genuinely that shape). Minimal in-scope fix to `chordTonePcs()`: a `q==='dim'` suffix now always gets a 4th tone at root+9 (the diminished 7th), not only when the literal suffix string ends in "7" - without it, exactly half the enharmonic names sharing one physical dim7 fret group (e.g. guitar's D#dim/Ebdim) honestly null because the bass note lands on an untracked chord tone. Symmetric-chord convention (documented in shape-classify.js and the test suite): inversion is reported relative to whichever chord NAME the caller asked about, never a claim about which note is objectively "the" root of a symmetric chord | [shared/shape-classify.js](../../shared/shape-classify.js), [ux-philosophy/expertise-adaptive-display.md](ux-philosophy/expertise-adaptive-display.md), [../../test/shape-classify.test.js](../../test/shape-classify.test.js) |
| D-EAR-1.6 | M-EAR wave 1.6 amends D-EAR-1 with a Slow/Med/Fast tempo control (defaulting FASTER than wave 1's implicit 72bpm); re-confirms (does not amend) the wave-1.5 sounding-light MECHANISM after live re-repro found no logic bug, but HARDENS its visibility (size/stroke/glow) AND re-derives its color (and the chord-targeting kx-* palette) from the user's own accent hue via CSS Relative Color Syntax; introduces the Legend primitive replacing the old target-caption prose; fixes a chord-card height/alignment regression for honest-null classifier results - KEEP-BOTH/AMEND-CAREFULLY with D-EAR-1/D-EAR-1.5/D-TARGET (this adds + hardens, does not replace any of them) | operator audition-round-2 UAT (docs/plans/uat-walkthrough-20260704.md U14-U21, verbatim: "needs faster tempo. add a slider for slow/med/fast control." / "I don't see the frets lighting up." / "the legend below needs specific color dots listed with description." / "a choice of colors of notes on the fretboard as they play... dark mode + accent colors... complementary and/or adjacent tones... yielding to you" / "chord Bdim missing [its shape label, card height off]"). **U14**: `Sound.playScale()` gains `handle.setTempo(bpm)` - SAME next-tick-boundary mechanics as `retarget()` (the in-flight note keeps its already-scheduled duration; only the FOLLOWING tick's interval changes, no click/gap). `TEMPO_BPM = {slow:72, med:104, fast:140}`, persisted via additive `music.tempo.v1` (default now `'med'` - directly answers "needs faster tempo" rather than shipping the control alone with the old slow default unchanged); the control reuses `.viewToggle` verbatim (same primitive U13's Window\|Full-neck toggle already established) - zero new CSS for the control itself. **U15**: live Playwright re-repro across window/full-neck x mode/pentMinor x target on/off found the D-EAR-1.5 mechanism fires correctly in EVERY combo - NOT a logic bug. The operator's "I don't see it" is explained by the visual delta being genuinely too subtle (light theme especially: the pre-U20 `--sound-bg` #efe7fb sat close in luminance to the default `--dg-dot` #dfe6ee fill, same dot radius, no other visual weight). Fix is a HARDEN: `.kx-sounding` gains `transform:scale(1.35)` (via `transform-box:fill-box`/`transform-origin:center` so the enlarged circle stays centered on its own, unscaled sibling `<text>`), a heavier `stroke-width:2.6`, and a `filter:drop-shadow` glow - a SIZE + STROKE + GLOW signal that reads regardless of hue perception. **U16**: new `shared/legend.js` (`window.Legend`, `render(classes) -> HTMLElement\|null`) - real kx-* swatch + label rows for a fixed 6-class vocabulary (root/chord/blue/ghost/rub/sounding), each swatch built from the SAME `var(--kx-*/--accent*/--dg-dot*/--sound-*)` tokens `diagram.js`'s actual fretboard dots use (never an approximated hex - a theme/accent change propagates to the legend for free). Replaces the old `renderTargetCaption()` prose sentence entirely; `tracks.js`'s new `legendClassesFor(bundle, scaleId, isSounding)` derives which classes are CURRENTLY VISIBLE from the SAME `computeTones()`/`defaultTones()` the fretboard render itself consumes (never a second, divergent notion of "what's on screen") plus the live `studioSound` state - `renderLegend()` is called at Studio open, every scale-chip `select()` (a genuine NEW requirement vs the old caption, which never varied by scale and so never needed re-rendering there), `toggleTarget()`, and sound play-start/stop. **U20** (folded into this wave per the capture note - "same surfaces"): the kx-chord/kx-blue/sound-bg/sound-line colors above are now DERIVED, not fixed hex - `tracks.css`/`songbook.css` each gain an `@supports (color: oklch(from red l c h))` block computing `oklch(from var(--accent) L C calc(h + offset))` (chord h+30, blue h-30 - adjacent on opposite sides; sounding h+180 - complementary, needing maximum perceptual distance as the one transient-state class; root stays `--accent` itself, ghost stays neutral). L/C are PINNED per theme (not derived from the accent's own L/C) at values chosen to match the prior literal hex figures' weight, so contrast holds across arbitrary accent hues without re-deriving from scratch; a literal-hex fallback (the pre-U20 values, unchanged) covers browsers without Relative Color Syntax support. Live-verified: a hue-picker change re-colors the real fretboard dots AND the Legend swatches to the IDENTICAL resolved color (same computed `fill`), both themes - see [ux-philosophy/component-conventions.md](ux-philosophy/component-conventions.md)'s "Note palette" table for the full offset/L/C spec and the shipping PR body for the before/after screenshots. `test/consistency-lint.test.js` gained a "(d) U20" block asserting every `@supports`-block color derives from `var(--accent)`, zero stray literal hex. **U21** (folded into this wave's region - shape-classify.js template curation for dim/aug itself is a SEPARATE, not-yet-built follow-up, S-DIM-SHAPES): a 'patterns'-mode chords-in-key card for an unclassifiable quality (shape-classify.js's honest null for dim/aug) rendered visibly shorter/misaligned next to its classified row-mates, because `Diagram.render()`'s label div only rendered when `patternLabel` was truthy - and an unclassifiable voicing's label is `''`, indistinguishable from plain 'dots'-mode `''` at the render level. Fix: new `opts.reserveLabelSlot` (boolean, absent/false byte-identical to pre-U21) - when true, an EMPTY label div still renders (`min-height:3.75em`, matched to shape-classify.js's typical 3-line label wrap at a small card's width); `chord-pack-adapter.js` sets it to `DiagramPref.get() === 'patterns'` on all 3 `Diagram.render()` call sites. Live-verified: a C-major Studio's 7 chords-in-key cards (incl. the vii° dim card) all measure the identical height in 'patterns' mode. Tap-to-hear on the dim card was ALSO live-verified firing correctly (no JS errors, the tap `.sel` flash fires) - `Circle.chordTones` already handles diminished quality; nothing to fix there | [systems/practice-studio.md](systems/practice-studio.md), [systems/ssot-registry.md](systems/ssot-registry.md), [ux-philosophy/component-conventions.md](ux-philosophy/component-conventions.md), [ux-philosophy/expertise-adaptive-display.md](ux-philosophy/expertise-adaptive-display.md), [../../test/sound.test.js](../../test/sound.test.js), [../../test/legend.test.js](../../test/legend.test.js), [../../test/consistency-lint.test.js](../../test/consistency-lint.test.js), [../../test/diagram.dom.test.js](../../test/diagram.dom.test.js), [../../test/live-adapter.test.js](../../test/live-adapter.test.js) |
| D-ENFORCE-2 | M-DESIGN-ENFORCE wave 2 (UAT U19 - operator screenshot of two stacked, inconsistent feedback surfaces on one action): app-wide feedback-surface AUDIT + reconcile onto the existing taxonomy, plus TWO additions to it - TOAST+ACTION (undoable-outcome toast, timed, countdown bar, pause-on-touch) and HELP (icon-marked always-available explainer, convention drafted). KEEP-BOTH in the D-TOAST-PRIMITIVE sense: ONE shared toast.js timing/bar primitive, but the setlist item-remove undo and the Compose Clear undo keep their OWN distinct visual placement (inline above the setlist body vs inline above the progression box), not forced into one shared visual component | `toast.js` gains `Toast.showAction(msg, opts)`/`Toast.wirePauseOnTouch(el, handle)` as a NEW entry point (not an option on `Toast.show()`) - the two SHIPPED plain-toast consumers stay byte-for-byte unchanged, pure addition to a SHIPPED primitive (same discipline D-TOAST-PRIMITIVE established). Default 6s window (operator ruling, up from an initial ~8s judgment-call draft - the visible countdown bar is what makes 6s feel responsive, not risky) with a `.toastBar` CSS-transition-driven countdown stripe (`.toastAction` host modifier: `position:relative;overflow:hidden`) and `prefers-reduced-motion` fallback (static full-width bar, no motion, same auto-hide timing). Pause-on-touch (`pause()`/`resume()` on the returned handle) stays DOM-agnostic in `showAction()` itself (a test can pass a plain-object host, matching `show()`'s own testability) - `wirePauseOnTouch()` is the opt-in real-DOM wiring so callers don't duplicate the touch/pointer-listener boilerplate. AMENDS the Undo contract (A3, interaction-safety.md): the banner used to be untimed ("stays until any mutating action invalidates it") - it is now ALSO time-bound; mutation-invalidation and the timer both end the pending undo, whichever fires first wins. Both live consumers (`showSetUndoBanner`/`showClearUndoBanner`, songbook.js) migrated to STABLE sibling elements (inserted once, not rebuilt inside their surface's repaint cycle) specifically so an unrelated re-render (a setlist reorder, an unrelated Compose edit) can never silently restart or leak the countdown. Separately, the Settings backup/restore flow's native `confirm()`/`alert()` calls (U19's named KNOWN offender) are replaced with `showSettingsToast()`/`openConfirmModal()` (play/index.html) - NEW host elements reusing the EXISTING TOAST/MODAL classes verbatim, confirming both primitives generalize beyond their original Compose-only call sites; this is the SECOND independent MODAL consumer. Native `confirm()` elsewhere (songbook.js delete-item/clear-setlist, repertoire-form.js delete) is registered debt, deliberately NOT fixed this wave (out of the U19 grant) - counts pinned by `test/no-native-dialog-lint.test.js` so they can't silently grow. The HELP-icon convention (`.helpIcon`, songbook.css) is DRAFTED but not yet applied - the app's only HELP-nature toggles (`.bt-st-why-toggle` family) live in tracks.js, a sibling mission's grant | [ux-philosophy/ui-primitives.md](ux-philosophy/ui-primitives.md), [ux-philosophy/component-conventions.md](ux-philosophy/component-conventions.md), [ux-philosophy/interaction-safety.md](ux-philosophy/interaction-safety.md), [systems/ssot-registry.md](systems/ssot-registry.md), [../../test/toast-action.test.js](../../test/toast-action.test.js), [../../test/no-native-dialog-lint.test.js](../../test/no-native-dialog-lint.test.js) |
| D-SET-INTEGRITY | S-SET-INTEGRITY closes the dangling-setlist-reference bug class at THREE levels, KEEP-ALL-THREE (belt-and-suspenders, not "pick one"): (1) delete-heal - `deleteCustomItem` (a custom song delete OR a fork revert) now gets the SAME TOAST+ACTION undo primitive D-ENFORCE-2 built, on the ONE OTHER call site that can silently orphan a setlist slot; (2) defensive nav - `Queue.stepResolvable()` (new, `shared/queue.js`) steps past a dangling ref instead of landing on it; (3) load-heal - `pruneDanglingSetlist()` (new, `shared/songbook.js`) silently prunes any surviving dangling ref at mount time, BEFORE the first render. Deliberately a READ-TIME defensive pass, not a `StorageMigrate` registration (see below) | UAT U22 (operator, 2026-07-05, screenshots): using Setlist Prev/Next lost the current song to the "Choose a song from the Library" empty state - traced to a set member deleted from the library (during U19 testing) leaving its id behind in the persisted setlist; `Queue`'s cursor doesn't validate membership, so navigating onto that id nulled `STATE.current` with no recovery. **(1) Delete-heal:** `deleteCustomItem` already pruned/remapped the setlist atomically (`remapSetlist`, D-HARDEN-era code, unchanged) but did so SILENTLY with zero undo - a native `confirm()` was, and remains, the only upstream guard (registered debt per D-ENFORCE-2 above). `showDeleteUndoBanner` (mirrors `showSetUndoBanner`'s stable-sibling-element pattern, inserted before `el.songsList`) now names what happened truthfully: `"Deleted <title>"` plain, or `"Deleted <title> - also removed from your setlist"` when it was setlisted (singular "your setlist" - this app has exactly ONE Jam setlist per profile, never plural), or `"Reverted <title> to the original"` for a fork (the set does NOT shrink there - the slot is replaced in place, not removed, so no "also removed" clause). Undo restores BOTH the customSongs record (spliced back at its original array index) AND the setlist slot (spliced back at its original index for a plain delete; the fork id is written back over wherever the catalog id now sits for a revert) - captured BEFORE the mutation, clamped at undo time (`Math.min(idx, arr.length)`) so an intervening setlist edit during the 6s window can't crash the restore, only slightly drift its position. A failed persist on Undo (quota/blocked storage) surfaces the existing `SAVE_FAIL_MSG` via the Library's OWN toast host (D-SAVE-TRUTH discipline, not a silent claim of success) - a different host than the undo banner itself, so no timer collision. **(2) Defensive nav:** `Queue.stepResolvable(dir, resolves)` generalizes `next()`/`prev()` with an injected resolver - steps past any id `resolves()` reports falsy for, same clamp-not-wrap contract, hard-bounded by list length so a pathological caller state can never spin forever; returns `{id, skipped}`. `songbook.js`'s `navQueue()` (Practice queue-nav) and the Stage `pPrev`/`pNext` handlers both call it with `songById` as the resolver and set a one-shot `STATE.queueSkipNotice` ("N removed song(s) skipped") appended to the SAME position-counter readout (`.qPos` / `el.pPos`) rather than a separate toast - "a calm inline notice," per spec, not a toast storm. Cleared to null on a skip-free nav, and on every fresh `openPractice`/`startPerform` launch so a stale notice never carries into an unrelated session. **(3) Load-heal:** `pruneDanglingSetlist(setlist, resolves)` mutates in place (same contract as `remapSetlist`) and runs once, immediately after `rebuildAll()` in mount-init (before `renderFilterChips`/`renderSongs`/`renderSetlist`), persisting via `saveSet()` if anything changed - so a setlist carrying a pre-fix dangling ref (or one restored from an older backup) is healed silently before the very first paint. **Why NOT a `StorageMigrate` registration:** `StorageMigrate.register(fromVersion, fn)` migrations run ONCE per device crossing a version boundary - correct for a SHAPE change, wrong for a data-integrity invariant that must hold on EVERY load regardless of when or how a dangling ref got there (a future bug, a restore, cross-device drift). A read-time defensive pass (matching the app's own "every reader is defensive" discipline, [data-model.md](systems/data-model.md)) is the correct mechanism; `music.schema.version`/`SCHEMA_VERSION` stay untouched (purely additive, no shape change, no bump). Live Playwright-verified (412x915, real served files, zero console errors in all three): the exact U22 repro (delete the middle of a 3-song set via the real "Delete progression" button, reopen the set, Next through where it used to be) now shows the truthful undo toast, a healed "1 / 2" counter (never a stale "1 / 3"), and lands cleanly on the last song with no empty-state fall-through; a genuinely orphaned id seeded directly into `roadcase-<profile>.setlist.v1` (simulating pre-fix data) is pruned by the very first render after a fresh reload, persisted back to storage; the real shipped `window.Queue.stepResolvable` (not a test double) correctly skips 3 synthetic dangling ids in a row | [systems/data-model.md](systems/data-model.md), [ux-philosophy/interaction-safety.md](ux-philosophy/interaction-safety.md), [../../test/queue.test.js](../../test/queue.test.js), [../../test/songbook.test.js](../../test/songbook.test.js) |
| D-TOAST-HOST | S-TOAST-HOST fixes the U24 empty-toast-host bug by adding the ONE missing CSS override (`.setUndo[hidden]{display:none;}`), NOT by redesigning the hide mechanism - KEEP-BOTH in the D-TOAST-PRIMITIVE sense: the DOM `hidden`-attribute toggle stays (`showSetUndoBanner`/`showDeleteUndoBanner` are unchanged), it just gets the same CSS companion every other `[hidden]`-toggled host already had | UAT U24 (operator, 2026-07-05, screenshot): "delete song from setlist leaves confirmation container empty." Root cause was a CSS cascade gap, not a JS lifecycle bug: `.setUndo{display:flex;...}` is an AUTHOR-origin rule, and author-origin ALWAYS outranks the user-agent's built-in `[hidden]{display:none}` regardless of selector specificity (the cascade resolves origin BEFORE specificity/order) - so `setUndoBanner.hidden = true` / `delUndoBanner.hidden = true` (both `paintSetUndoHidden`/`paintDelUndoHidden`, unchanged) had ZERO visual effect. The banner's content was correctly cleared (`innerHTML = ''`) but the box itself stayed on screen as a visible, empty pill - exactly the operator's screenshot. `.composeToast[hidden]{display:none;}` (D-ENFORCE-2 era) and `.composeRow[hidden]{display:none;}` already existed for the OTHER two `[hidden]`-toggled toast hosts (`composeToast`, `clearUndoBanner`) - `.setUndo` (shared by BOTH `setUndoBanner`, the setlist item-remove undo, AND `delUndoBanner`, the Library delete-undo - D-SET-INTEGRITY) was the one instance of the pattern that got missed when D-ENFORCE-2/D-SET-INTEGRITY migrated these banners onto the `hidden` attribute. Live Playwright-verified (412x915, real served files) both BEFORE (repro: a visible empty `.setUndo.toastAction` box, `hidden=true` but `display:flex`, persists after the 6s countdown on both the setlist-remove path and the Library delete path) and AFTER (the box now genuinely reaches `display:none`/`offsetParent:null` the instant the toast lifecycle ends, on both paths, and the "Undo" button still restores the exact prior position/index in both). The "shows too briefly" half of the operator's report did not reproduce as a SEPARATE timing bug - `Toast.showAction`'s 6s countdown and `.toastBar` animation run to completion unchanged in every repro; the perception is fully explained by the SAME bug (a confirmation that visibly empties out but never disappears reads as "it vanished early, then left something behind"). New `test/toast-host-visibility-lint.test.js` statically enumerates every host ever passed to `Toast.show()`/`Toast.showAction()` in songbook.js and asserts each `hidden`-attribute-controlled one has a matching CSS `[hidden]{display:none;}` override (and separately confirms `toastEl`, the one opacity/class-toggle host, keeps its own `.toast{opacity:0}`/`.toast.on{opacity:1}` pair) - reproduces the bug when run against the pre-fix CSS, so a future new toast host missing its `[hidden]` companion fails loud in CI instead of shipping a silent empty-pill regression again | [ux-philosophy/ui-primitives.md](ux-philosophy/ui-primitives.md), [../../test/toast-host-visibility-lint.test.js](../../test/toast-host-visibility-lint.test.js), [../../docs/plans/uat-walkthrough-20260704.md](../../docs/plans/uat-walkthrough-20260704.md) |

## Related

- [index.md](index.md) - routing table
- [AGENTS.md](AGENTS.md) - markers + read order
- [workflows/roadmap-missions.md](workflows/roadmap-missions.md) - phases + backlog
